# Crucix dev cycle 086 - direct cluster provenance attribution

## Plan target
- Epic 26, effort 26.5
- Add direct per-cluster source provenance or cluster-to-runtime-source attribution so source contribution scoring stops relying on feed-type proxies like RSS to GDELT and can measure downstream quality impact more truthfully.

## What changed
- Added per-cluster `sourceProvenance` summaries in `dashboard/inject.mjs`.
- Included publisher-level source, item type, runtime-attributed source, counts, and top-source rollups on cluster payloads.
- Extended `server.mjs` brief/news summaries so operator-visible cluster surfaces carry `sourceProvenance`.
- Updated `lib/source-ops-runtime.mjs` so source performance scoring now consumes direct cluster attribution, tracks `clusteredItems` and `clusteredStories`, and avoids overcounting multiple publisher entries inside the same cluster.
- Expanded tests to lock the new provenance and attribution contract.

## Validation
- Focused suite passed:
  - `node --test test/news-placement-classes.test.mjs test/news-cluster-review-metrics.test.mjs test/source-ops-runtime.test.mjs test/reasoning-endpoint-contract.test.mjs test/source-registry.test.mjs test/agent-analysis-validation.test.mjs`
- Live endpoint proof after listener refresh:
  - `/api/brief/news` now returns cluster `sourceProvenance` entries including `runtimeSource`.
  - `/api/health` now reports non-zero `sourceOps.performance.withClusterAttribution` and shows `GDELT` with `clusteredItems` and `clusteredStories` contribution.

## Notes
- The existing backlog item for a dev-safe restart helper remains valid. Local validation still occasionally hits ambiguous port `3117` listener handoff behavior during restarts.
