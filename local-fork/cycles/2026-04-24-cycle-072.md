# Crucix dev cycle 072

- Date: 2026-04-24
- Plan item: Epic 23, effort 23.3
- Title: Expose an operator-facing source inventory and source-ops needs surface showing lifecycle distribution, current gaps, and actionable queue pressure

## What changed

- Added a shared runtime helper at `lib/source-ops-runtime.mjs` that reads the source-ops workspace files and builds an operator-facing surface for:
  - source-ops contract policy
  - source inventory summary
  - per-source live-state attachment when source health data exists
  - source-ops runtime needs summary
- Exposed the new surface in the existing runtime APIs:
  - `/api/data`
    - adds `sourceInventory`
    - adds full `sourceOps`
  - `/api/brief/compact`
    - adds compact `sourceInventory`
    - adds `sourceOpsNeeds`
  - `/api/health`
    - adds `sourceInventory`
    - adds full `sourceOps`
- Kept the implementation additive so existing consumers are not broken.

## Validation

- Passed:
  - `node --check lib/source-ops-runtime.mjs`
  - `node --check server.mjs`
  - `node --test test/source-registry.test.mjs test/source-ops-contract.test.mjs test/source-ops-runtime.test.mjs`
- Live runtime validation against `http://127.0.0.1:3117` confirmed:
  - `/api/health` returns `sourceOps` and `sourceInventory`
  - `/api/data` returns `sourceOps` and `sourceInventory`
  - `/api/brief/compact` returns `sourceOpsNeeds` and compact `sourceInventory`
- Observed live values after deploy:
  - `sourceInventory.total = 29`
  - `sourceOps.needs.total = 2`
  - `sourceOps.contract.contractMode = file-first`
  - `sourceOps.contract.activePromotionRequiresHumanApproval = true`

## Runtime note

- One restart attempt logged a transient `port 3117 is already in use` race during handoff, but the active listener was verified afterward as:
  - `/opt/homebrew/opt/node/bin/node /Users/rightclaw/services/crucix/server.mjs`
  - PID `77548`
- Live endpoint checks confirmed the updated runtime is serving the new source-ops surfaces.

## Backlog follow-up

- No new backlog item added this cycle.
- The next planned work after this remains the roadmap beyond Epic 23.3.
