# Crucix Dev Cycle 120 - Noise-suppression telemetry surfacing

- Date: 2026-04-26
- Plan item: `epic-29 / effort-29.6`
- Status: completed

## Goal
Surface noise-suppression history decay and prune telemetry in health and review diagnostics so operators can see when stale suggestion weight has aged out, how many retained history entries remain, and whether pruning is actively keeping state bounded.

## Changes shipped
- Updated `server.mjs`:
  - added persisted prune telemetry under the noise-suppression history envelope
  - added decay telemetry summarizing aged-out suggestion weight and retained per-bucket counts
  - exposed noise-suppression telemetry on `/api/health` as `noiseSuppressionTelemetry`
  - exposed decay and prune telemetry through the existing review workflow noise-suppression history contract
- Updated `dashboard/public/diagnostics.html`:
  - surfaced noise-suppression history telemetry in diagnostics
  - shows aged-out suggestion weight, retained versus total history entries, pruning-active state, expired versus overflow removals, and per-bucket retained/aged-out counts
  - shows per-candidate history and decayed weight for duplicate-burst and repetitive-low-value items
- Updated tests:
  - `test/cluster-repair-workflow.test.mjs`
  - `test/settings-endpoint-contract.test.mjs`
- Updated `local-fork/BACKLOG.md`:
  - added a follow-on recommendation for rolling per-sweep telemetry snapshots and a diagnostics trend view

## Validation
- Tests passed:
  - `node --test test/cluster-repair-workflow.test.mjs test/settings-endpoint-contract.test.mjs`
- Live `3117` evidence:
  - listener rolled to PID `48722`
  - `/api/health` exposed:
    - `noiseSuppressionTelemetry.version = "noise-suppression-history-v2"`
    - `noiseSuppressionTelemetry.decayTelemetry.agedOutSuggestionCount = 7`
    - `noiseSuppressionTelemetry.pruneTelemetry.summary.retainedEntries = 15`
    - `noiseSuppressionTelemetry.pruneTelemetry.pruningActive = false`
  - `/api/brief/news/review` exposed matching history telemetry values under `workflow.noiseSuppression.history`
  - `/diagnostics` rendered the new aged-out suggestion weight text
- Restart helper behavior:
  - `npm run restart:safe` again exited with `Port 3117 still has a listener after waiting for shutdown`
  - despite that conservative failure, the live listener did roll and served the updated telemetry afterward

## Backlog
- Added unscheduled follow-on item:
  - add rolling per-sweep noise-suppression telemetry snapshots and a small diagnostics trend view so operators can tell whether aged-out history and prune activity are one-off events or a sustained state-growth pattern over time

## Notes
- The live runtime showed telemetry surfacing correctly, but `pruningActive` was `false` for the sampled sweep because no prune/removal happened in that live cycle.
