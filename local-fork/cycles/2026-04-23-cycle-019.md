# Crucix dev cycle 019

- Date: 2026-04-23
- Objective: execute Epic 3 effort 3.3, add any follow-on recommendation to the backlog, and validate the live runtime
- Status: completed

## Planned work

1. Inspect the current LLM clustering flow and identify the control and fallback signals to expose.
2. Add any new unscheduled recommendation to the backlog.
3. Implement LLM cluster operational controls and fallback visibility on the runtime.
4. Validate the live runtime, record the cycle, and commit the repo.

## Results

- Advanced the active roadmap effort in `local-fork/plan.json`:
  - moved `effort-3.3` to `in_progress` at cycle start
- Added a new unscheduled recommendation to `local-fork/BACKLOG.md`:
  - per-region LLM cluster retry/backoff counters so operators can separate transient model failures from chronically weak candidate sets
- Implemented LLM cluster control and fallback visibility in `dashboard/inject.mjs`:
  - `consolidateNewsWithLLM()` now tracks `requestedMode`, `providerConfigured`, `attempted`, `used`, `fallbackReason`, candidate-set counts, success/error counts, fallback counts, and per-region status
  - `buildNewsClusters()` now accepts a runtime `mode` option (`auto`, `off`, `force`)
- Exposed the new operator-facing control surface in `server.mjs`:
  - `buildNewsClusterSummary()` now returns detailed LLM metadata instead of only provider and candidate count
  - `/api/brief/news` now supports `?llm=auto|off|force`
  - compact briefing text now includes a `News LLM: ...` line summarizing whether clustering used LLM output or fell back to heuristics
- Fixed a real runtime startup issue discovered during validation:
  - startup no longer blocks the initial sweep while trying to synthesize `runs/latest.json`
  - brief/data endpoints no longer pretend to be ready until `currentData` actually exists

## Evidence

- Direct helper validation confirmed the control path:
  - `buildNewsClusters(sample, null, { mode: 'off' })` returned `requestedMode: off` and `fallbackReason: operator-disabled`
- Live runtime verification confirmed:
  - `/api/brief/compact` now includes `News LLM: mode auto, heuristic fallback, candidates 6, fallbacks 6`
  - `/api/brief/news` returned:
    - `llm.requestedMode: auto`
    - `provider: ollama`
    - `attempted: true`
    - `used: false`
    - `fallbackReason: all-candidate-sets-fell-back`
    - `candidateSetCount: 6`
    - `heuristicFallbackCount: 6`
    - region-level fallback entries such as `Iran -> heuristic-fallback -> no-json-match`
  - `/api/brief/news?llm=off` returned:
    - `llm.requestedMode: off`
    - `fallbackReason: operator-disabled`
    - `candidateSetCount: 0`
- Active runtime check:
  - `/api/health` returned `status: ok`
  - the initial sweep now starts immediately after restart instead of idling behind a blocking startup synth

## Notes

- The live result is clear: the LLM clustering path is configured and attempted, but on this sweep every candidate set fell back to heuristics because the model did not return parseable JSON for the cluster prompt.
- Epic 3 is now functionally complete as planned. The next best work is probably either scheduling the new retry/backoff counters or opening a new epic for cluster prompt/response hardening.
