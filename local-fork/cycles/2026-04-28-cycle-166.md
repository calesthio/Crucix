# Crucix Dev Cycle 166 - Generalized bounded runtime action feedback beyond restart-safe

- Date: 2026-04-28
- Plan item: `epic-51 / effort-51.1`
- Status: completed

## Goal
Generalize the shared runtime-action feedback path so bounded local runtime actions can report queued, completed, and failed states consistently instead of only treating restart-safe as a first-class audited action.

## Backlog review and promotions
- Reviewed `local-fork/BACKLOG.md` first.
- Promoted the remaining unscheduled backlog item into the roadmap before implementation:
  - `backlog-2026-04-28-admin-preset-presentation-controls` -> `epic-61 / effort-61.1`
- Also corrected roadmap bookkeeping so `epic-49` now reflects its already-completed `effort-49.1`, and cycle state now advances through cycle 165 before this cycle closes as 166.

## Changes shipped
- Generalized the shared ops-shell poll helper from restart-only semantics to runtime-action semantics while keeping a compatibility alias for existing callers.
- Extended runtime control queuing so `stop` now writes the same retained queued/completed audit trail shape that `restart-safe` already used.
- Updated admin and diagnostics surfaces to present runtime action audit history/timelines instead of restart-only wording, while still showing restart-specific details like listener rotation when present.
- Kept the retained audit payload under the existing runtime control contract so current consumers remain compatible, but broadened the contract notes and UI rendering to include `restart-safe` and `stop` consistently.
- Expanded targeted regression coverage for:
  - retained runtime action audit ordering
  - admin/settings surface wording and helper wiring
  - diagnostics surface wording and helper wiring

## Validation
- Tests passed:
  - `node --test test/runtime-restart-helper.test.mjs test/settings-contract.test.mjs test/settings-endpoint-contract.test.mjs test/ui-payload-contract.test.mjs`
  - result: `pass 15`, `fail 0`
- Live runtime rotation passed:
  - `npm run restart:safe`
  - live listener PID: `51442`
  - replacement mode: `replacement-detected`
  - rotation proved: `true`
- Representative live checks after restart:
  - `/api/health` reported `status: ok`, `ready: true`, current PID `51442`
  - `/api/settings/admin` reported runtime action audit version `runtime-restart-audit-v1` and latest retained entry `restart-safe / completed / ok`

## Backlog and follow-up
- No new unscheduled backlog items were added in this cycle.

## Notes
- This slice improves honest feedback and retained audit consistency for bounded runtime actions without widening the allowed runtime control action set.
- The retained audit name is still `restartAudit` for compatibility, but the entries and UI now intentionally cover bounded runtime actions more generally.
