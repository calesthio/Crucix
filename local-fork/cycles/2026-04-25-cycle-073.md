# Crucix dev cycle 073

- Date: 2026-04-25
- Plan item: Epic 24, effort 24.1
- Title: Create bounded OpenClaw subagent role definitions and task templates for source discovery, validation, grading, overlap analysis, pruning review, and onboarding preparation

## What changed

- Completed the bounded source-ops role set under `source-ops/roles/` by adding:
  - `validation.md`
  - `overlap.md`
  - `pruning.md`
  - `onboarding-prep.md`
- Kept the existing `discovery.md` and `grading.md` role definitions as the base of the full role pack.
- Added example task templates under `source-ops/tasks/` for every allowed role:
  - `example-validation-task.json`
  - `example-grading-task.json`
  - `example-overlap-task.json`
  - `example-pruning-task.json`
  - `example-onboarding-prep-task.json`
- Added results-directory scaffolding for each role under `source-ops/results/` so bounded subagents have a canonical write target per workflow.
- Expanded `test/source-ops-contract.test.mjs` so the contract now fails if any allowed role is missing either:
  - a bounded role definition, or
  - a matching task template aligned to the file-first, no-production-mutations policy.

## Validation

- Passed:
  - `node --check test/source-ops-contract.test.mjs`
  - `node --test test/source-ops-contract.test.mjs`
- Validation confirms:
  - every allowed source-ops role has a role definition file
  - every allowed source-ops role has a matching example task packet
  - every template keeps `contractMode = file-first`
  - every template keeps `productionMutationsAllowed = false`
  - every template keeps `activePromotionRequiresHumanApproval = true`

## Backlog follow-up

- No new backlog item added this cycle.
- The next planned work is `24.2`: implement the structured source scorecard and explainable grading rubric.
