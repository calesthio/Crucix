# Crucix Dev Cycle 137 - Added end-to-end review-workflow endpoint regression coverage

- Date: 2026-04-27
- Plan item: `epic-34 / effort-34.5`
- Status: completed

## Goal
Add end-to-end regression coverage for `POST /api/review-workflow/action` and `GET /api/review-workflow/audit`, including action payload validation and audit-entry assertions across the bounded review workflow surface.

## Changes shipped
- Added `test/review-workflow-endpoint.test.mjs`.
- The new test boots a real Crucix server and validates live review-workflow endpoint behavior for:
  - `ack`
  - `snooze`
  - `ack-noise-suppression-pressure`
  - `promote-shadow`
  - bounded source action application when a source action is available from queue items
  - bounded cluster repair action application when a weak cluster exposes one
- The test also validates `GET /api/review-workflow/audit` and asserts expected audit-entry recording for executed actions.
- Updated `local-fork/plan.json` to mark `effort-34.5` complete and advance local cycle tracking to cycle 137.

## Validation
- Tests passed:
  - `node --test test/review-workflow-endpoint.test.mjs test/settings-endpoint-contract.test.mjs test/review-queue.test.mjs`
- Live runtime validation passed on the actual local Crucix service:
  - `GET http://127.0.0.1:3117/api/health` returned `status = "ok"`, `phase = "serving"`, `dataReady = true`, `pid = 61357`, `port = 3117`
  - `GET http://127.0.0.1:3117/api/data` returned `reviewWorkflow.version = "review-workflow-v1"`, `reviewQueue.state = "empty_elevated_metrics"`, `reviewQueue.totalItems = 0`

## Backlog and roadmap follow-up
- No new backlog items were added.
- The next planned slice is `epic-34 / effort-34.6` for endpoint-level regression coverage around cluster-repair workflow actions and suppressed-cluster state on `/api/brief/news/review` and `/api/data`.

## Notes
- This cycle strengthens live endpoint contract protection for the bounded review workflow without requiring a production behavior change.
- The test is written to validate optional source-action and cluster-action paths when the live booted snapshot exposes them, while still always covering the core audit-bearing workflow actions.
