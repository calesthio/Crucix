# Crucix Dev Cycle 176 - Add browser regression coverage for admin preset flows

- Date: 2026-04-29
- Plan item: `epic-58 / effort-58.1`
- Status: completed

## Goal
Add browser-level regression coverage for admin preset clone, import, and delete flows so the operator-editable preset authoring path stays honest.

## Backlog review and promotions
- Reviewed `local-fork/plan.json` and found no remaining ready backlog items to promote.
- Tidied `local-fork/plan.json` by clearing completed-only backlog entries and syncing epic statuses to their completed effort state.
- No new backlog items were added in this cycle.

## Changes shipped
- Fixed a live syntax error in `dashboard/public/admin-settings.html` that prevented the admin settings page from fully rendering in a browser.
- Added `test/admin-preset-browser-regression.test.mjs` to exercise browser-rendered admin preset clone, import, delete, and persistence behavior against a booted Crucix server.
- Marked `epic-58 / effort-58.1` completed in `local-fork/plan.json` and advanced local cycle state to cycle 176.

## Validation
Executed:
- `node --test --test-concurrency=1 test/admin-preset-browser-regression.test.mjs test/ops-shell-browser-contract.test.mjs test/operator-settings-persistence.test.mjs test/local-fork-planning-hygiene.test.mjs test/served-page-smoke.test.mjs`
- `npm run restart:safe`
- live checks against `http://127.0.0.1:3117/api/health`, `http://127.0.0.1:3117/api/settings/admin`, and `http://127.0.0.1:3117/admin/settings`

Results:
- Tests passed: `pass 6`, `fail 0`
- Runtime restart passed:
  - previous listener PID: `12045`
  - live listener PID after rollout: `16736`
  - replacement mode: `replacement-detected`
  - rotation proved: `true`
  - listener lookup: `lsof`
- Live endpoint evidence after restart:
  - `/api/health` returned `status: ok`
  - `/api/health` returned `lifecycle.phase: serving`
  - `/api/health` returned `lifecycle.dataReady: true`
  - `/api/health` returned `runtimeIdentity.pid: 16736`
  - `/api/settings/admin` returned `persistence.revision: 19`, `persistence.etag: "operator-settings-r19"`, and `admin.writeAuth.required: true`
  - live `/admin/settings` HTML includes `clonePresetBtn`, `importPresetBtn`, and `deleteCustomPresetBtn`

## Runtime note
- `/api/health` still reports `sweepInProgress: true` and `lastSweep: null` while the runtime is otherwise serving normally. I treated that as pre-existing runtime freshness behavior outside this bounded admin-preset regression slice.
