# Crucix Dev Cycle 140 - Added critical-event taxonomy and policy contract

- Date: 2026-04-27
- Plan item: `epic-35 / effort-35.1`
- Status: completed

## Goal
Define the first critical-event policy contract for fast-alert classes, including per-class severity, corroboration thresholds, freshness windows, and route defaults, without yet shipping candidate promotion or proactive paging.

## Changes shipped
- Extended operator settings defaults and normalization to persist `alerts.criticalEvents` policy state.
- Added `buildCriticalEventPolicyContract()` and exposed it on `/api/settings` as `alerts.criticalEvents`.
- Included the first taxonomy classes:
  - `governmentSiteViolence`
  - `aviationIncident`
  - `radiationAnomaly`
  - `chokepointDisruption`
- Added per-class policy controls for:
  - `enabled`
  - `severity`
  - `minHighTrustCorroboration`
  - `minMediumTrustCorroboration`
  - `officialConfirmationRequired`
  - `freshnessMinutes`
- Fixed settings bundle import for review-ack state by restoring `normalizeReviewAckEntry()`, which the import path referenced but did not define.
- Updated tests to cover critical-event policy defaults, persistence, export/import survivability, and live settings endpoint exposure.
- Updated `local-fork/plan.json` to mark `effort-35.1` complete and advance local cycle tracking to cycle 140.

## Validation
- Tests passed:
  - `node --test test/settings-contract.test.mjs test/operator-settings-persistence.test.mjs test/settings-endpoint-contract.test.mjs test/ui-payload-contract.test.mjs`
- Live runtime validation passed on the actual local Crucix service after redeploy:
  - `GET http://127.0.0.1:3117/api/health` returned `status = "ok"`, `phase = "serving"`, `dataReady = true`, `pid = 68631`
  - `GET http://127.0.0.1:3117/api/settings` returned `version = "operator-settings-v1"`, `alerts.criticalEvents.version = "critical-event-policy-v1"`, `alerts.criticalEvents.taxonomy.length = 4`
  - `GET http://127.0.0.1:3117/api/settings/admin` remained available after the redeploy on the same local runtime

## Backlog and roadmap follow-up
- Added backlog item `backlog-2026-04-27-runtime-restart-verification` to harden `restart-safe` so it proves PID rotation or reports failure explicitly.
- The next planned slice is `epic-35 / effort-35.2` for the urgent candidate queue and short-cadence corroboration retries.

## Notes
- This cycle intentionally stops at taxonomy and operator-visible policy contract exposure. It does not yet promote live candidates or send proactive notifications.
- The runtime control `restart-safe` path acknowledged a restart request but left the old PID serving stale contract output, so I completed deployment with an explicit stop/start and captured that gap in backlog instead of hand-waving it away.
