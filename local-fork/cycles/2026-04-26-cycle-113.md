# Crucix Dev Cycle 113 - Attribution uncertainty and alias-collision diagnostics

- Date: 2026-04-26
- Plan item: `epic-32 / effort-32.3`
- Status: completed

## Goal
Expose attribution uncertainty and alias-collision diagnostics so inferred runtime-source mappings, duplicate publisher aliases, and double-count risks are visible to operators.

## Changes shipped
- Added `buildAttributionUncertaintyDiagnostics(...)` in `lib/source-ops-runtime.mjs`.
- Extended the source performance workflow contract with `workflow.attributionDiagnostics`:
  - `version = "source-attribution-diagnostics-v1"`
  - summary counts for alias collisions, ambiguous mappings, double-count risks, uncertain clusters, and source caveats
  - top alias-collision groups by runtime source
  - ambiguous source-to-runtime mappings
  - bounded double-count risk items derived from cluster provenance
  - top uncertain clusters and source-level caveat highlights
- Extended `buildOperatorSettingsContract(...)` in `server.mjs` so `/api/settings` exposes the new diagnostics block through `sourceConsole.performanceWorkflow.attributionDiagnostics`.
- Updated `dashboard/public/source-ops.html` to render an `Attribution uncertainty diagnostics` section with:
  - aggregate summary counts
  - alias-collision visibility
  - ambiguous mapping visibility
  - double-count risk examples
- Extended regression coverage in:
  - `test/source-ops-runtime.test.mjs`
  - `test/settings-contract.test.mjs`
  - `test/settings-endpoint-contract.test.mjs`

## Validation
- Tests passed:
  - `node --test test/source-ops-runtime.test.mjs test/settings-contract.test.mjs test/settings-endpoint-contract.test.mjs`
- Live `3117` evidence:
  - `/api/settings` exposed `sourceConsole.performanceWorkflow.attributionDiagnostics.version = "source-attribution-diagnostics-v1"`
  - `/api/settings` exposed summary counts including `aliasCollisionCount = 1` and `doubleCountRiskCount = 47`
  - `/source-ops` rendered `Attribution uncertainty diagnostics`
  - `/source-ops` rendered `Alias collisions`
  - `/source-ops` rendered `Double-count risks`
- Restart behavior:
  - `npm run restart:safe` again reported `Port 3117 still has a listener after waiting for shutdown`
  - despite that conservative failure, the live listener did roll to PID `17697` and served the updated diagnostics contract afterward

## Backlog
- Added a follow-on item to distinguish expected aggregator publisher mixes from true alias-collision anomalies in `local-fork/BACKLOG.md`.

## Notes
- The first live cut is useful for surfacing provenance ambiguity and double-count risk, but alias-collision counts still include routine multi-publisher aggregator buckets like `GDELT`. That is now explicitly tracked as follow-on work rather than left implicit.
