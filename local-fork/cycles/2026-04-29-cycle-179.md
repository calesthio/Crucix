# Crucix Dev Cycle 179 - Admin preset presentation controls

- Date: 2026-04-29
- Plan item: `epic-61 / effort-61.1`
- Status: completed

## Goal
Extend local admin preset authoring so custom presets can explicitly edit `densityMode` and `topbarMode` instead of inheriting only built-in presentation defaults.

## Backlog review and promotions
- Reviewed `local-fork/plan.json` and found no remaining ready backlog items to promote.
- Completed `epic-61 / effort-61.1` and advanced local cycle state to cycle 179.
- No new backlog items were added in this cycle.

## Changes shipped
- Extended server-side operator settings normalization and exported settings payloads to persist top-level `densityMode` and `topbarMode` alongside custom preset fields.
- Updated the local admin settings surface so preset fallback metadata, form load/save flows, and preset import/export all carry `densityMode` and `topbarMode`.
- Added regression coverage for persisted operator settings and browser-driven custom preset import flows so both presentation controls stay round-trippable.
- Updated `local-fork/plan.json` to mark Epic 61 complete.

## Validation
Executed:
- `node --test --test-concurrency=1 test/operator-settings-persistence.test.mjs test/admin-preset-browser-regression.test.mjs test/settings-contract.test.mjs test/ui-payload-contract.test.mjs test/served-page-smoke.test.mjs`
- `npm run restart:safe`
- live checks against `http://127.0.0.1:3117/api/health`, `http://127.0.0.1:3117/api/data`, and `http://127.0.0.1:3117/api/settings`

Results:
- Tests passed: `pass 13`, `fail 0`
- Runtime restart passed:
  - previous listener PID: `25581`
  - live listener PID after rollout: `29989`
  - replacement mode: `replacement-detected`
  - rotation proved: `true`
  - listener lookup: `lsof`
- Live endpoint evidence after restart:
  - `/api/health` returned `status: ok`
  - `/api/health` returned `lifecycle.phase: serving`
  - `/api/health` returned `lifecycle.dataReady: true`
  - `/api/health` returned `runtimeIdentity.pid: 29989`
  - `/api/health` returned `sourceOps.inventory.total: 30`
  - `/api/settings` returned `layout.controls.workspacePreset: executive-briefing`
  - `/api/settings` returned `layout.controls.densityMode: balanced`
  - `/api/settings` returned `layout.controls.topbarMode: standard`
  - `/api/data` returned `criticalEventQueue.candidates.length: 2`

## Runtime note
- `/api/data` did not expose a top-level `newsCount` or layout control snapshot in this live sample, so validation relied on `/api/health` and `/api/settings` for concrete runtime evidence.