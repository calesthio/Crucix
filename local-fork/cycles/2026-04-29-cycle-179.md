# Crucix Dev Cycle 179 - Admin preset presentation controls

- Date: 2026-04-29
- Plan item: `epic-61 / effort-61.1`
- Status: completed

## Goal
Extend local admin preset authoring so custom presets can explicitly edit `densityMode` and `topbarMode` instead of inheriting only built-in presentation defaults.

## Backlog review and promotions
- Reviewed `local-fork/plan.json` and found no remaining backlog items ready for promotion.
- Backlog remained empty.
- No new backlog items were added in this cycle.

## Changes shipped
- Extended admin preset authoring in `dashboard/public/admin-settings.html` so custom preset clone, import, export, load, and save flows preserve `densityMode` and `topbarMode`.
- Added explicit `Density mode` and `Topbar mode` controls to the admin settings layout card.
- Extended operator settings normalization and layout controls contract in `server.mjs` to persist and expose `densityMode` and `topbarMode` for both live layout settings and custom presets.
- Extended regression coverage in `test/admin-preset-browser-regression.test.mjs` and `test/operator-settings-persistence.test.mjs`.
- Marked `epic-61 / effort-61.1` complete in `local-fork/plan.json` and advanced cycle state to 179.

## Validation
Executed:
- `node --test --test-concurrency=1 test/admin-preset-browser-regression.test.mjs`
- `node --test --test-concurrency=1 test/operator-settings-persistence.test.mjs`
- live checks against `http://127.0.0.1:3117/api/health`

Results:
- Tests passed:
  - `test/admin-preset-browser-regression.test.mjs` => `pass 1`, `fail 0`
  - `test/operator-settings-persistence.test.mjs` => `pass 1`, `fail 0`
- Live runtime evidence:
  - `/api/health` returned `status: ok`
  - `/api/health` returned `lifecycle.phase: serving`
  - `/api/health` returned `lifecycle.dataReady: true`
  - `/api/health` returned `runtimeIdentity.pid: 29989`

## Runtime note
- `/api/health` still reports `sweepInProgress: true` and `lastSweep: null` while the runtime is otherwise serving normally. I treated that as pre-existing freshness behavior outside this bounded preset-controls slice.
