# Crucix Dev Cycle 100 - Source lifecycle actions and policy blockers

- Date: 2026-04-26
- Plan item: `epic-30 / effort-30.2`
- Status: completed

## Goal
Add source lifecycle actions for candidate, shadow, active, quarantine, and prune recommendations with visible policy blockers and human-approval boundaries.

## Changes shipped
- Extended `sourceConsole` with lifecycle action metadata:
  - `lifecycleActions.version = "source-lifecycle-actions-v1"`
  - visible human-approval boundary states
  - visible auto-advance states
  - queue blocker counts and advanceable counts
  - normalized batch lifecycle evaluations
  - normalized example lifecycle evaluation
  - normalized prune recommendation summary
- Normalized lifecycle-action contract objects so they stay structurally stable even in reduced test contexts where example artifacts may be absent.
- Updated `/source-ops` to surface:
  - blocked lifecycle action count
  - advanceable candidate count
  - human-approval boundary badges
  - example lifecycle evaluation and blocked reasons
  - prune recommendation and blocking issues
- Kept lifecycle mutations honest: this slice surfaces policy blockers and approval boundaries, but does not pretend operator-read surfaces can bypass the local admin/human review boundary.

## Validation
- Tests passed:
  - `node --test test/settings-contract.test.mjs test/settings-endpoint-contract.test.mjs test/runtime-restart-helper.test.mjs`
- Live validation passed after safe restart:
  - `npm run restart:safe`
  - helper restarted owned listener from PID `6076` to PID `10809`
- Live `3117` evidence:
  - `lifecycleActionsVersion = "source-lifecycle-actions-v1"`
  - `humanApprovalBoundaryStates = ["shadow", "approved", "active", "degraded"]`
  - `autoAdvanceStates = ["candidate", "researched", "graded", "deprecated", "rejected"]`
  - `queueBlockedCount = 1`
  - `queueAdvanceableCount = 0`
  - `exampleRecommendedAction = "human-review"`
  - `pruneRecommendedAction = "human-review"`
  - `/source-ops` rendered blocked lifecycle actions, prune recommendation, and review-boundary badges

## Notes
- This slice is about visible lifecycle actions and blockers, not silent mutation power. Human-approval boundaries remain explicit instead of being watered down into a fake “operator action” button.
- No new unscheduled backlog item was added this cycle.
