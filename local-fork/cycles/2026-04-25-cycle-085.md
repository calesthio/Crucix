# Crucix dev cycle 085

- Date: 2026-04-25
- Plan item: Epic 26, effort 26.4
- Title: Add endpoint-level contract coverage for reasoning metadata on `/api/analysis` and `/api/brief/news` so source-role context cannot silently drop from live operator surfaces

## What changed

- Added a booted-server endpoint contract test in `test/reasoning-endpoint-contract.test.mjs`.
- The new test verifies the actual HTTP payloads, not just helper functions, for:
  - `/api/analysis`
  - `/api/brief/news`
- The endpoint contract now has explicit regression coverage for `sourceReasoning`, including:
  - total source count
  - anchor and exploratory counts
  - caution roles
  - grounding priority order

## Validation

- Passed:
  - `node --test test/reasoning-endpoint-contract.test.mjs test/source-ops-runtime.test.mjs test/source-registry.test.mjs test/news-cluster-review-metrics.test.mjs test/agent-analysis-validation.test.mjs test/imessage-provenance-brief.test.mjs`
- Test result:
  - `pass 19`
  - `fail 0`
- Live runtime validation confirmed against the actual `3117` listener:
  - `/api/analysis` exposed `agentAnalysis.sourceReasoning.totalSources = 30`
  - `/api/analysis` exposed `agentAnalysis.sourceReasoning.guidance.cautionRoles = ["exploratory"]`
  - `/api/brief/news` exposed `sourceReasoning.totalSources = 30`
  - `/api/brief/news` exposed `sourceReasoning.guidance.groundingPriority = ["anchor", "corroborator", "anomaly-detector", "context", "exploratory"]`

## Backlog follow-up

- No new backlog item was added this cycle.
- The direct per-cluster source provenance item remains scheduled as `26.5`.
- Next planned work is `26.5`: add direct per-cluster source provenance or cluster-to-runtime-source attribution so source contribution scoring stops relying on feed-type proxies.
