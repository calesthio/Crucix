# Crucix Dev Cycle 167 - Exposed critical-event classifier basis and ambiguity flags for operator review

- Date: 2026-04-28
- Plan item: `epic-52 / effort-52.1`
- Status: completed

## Goal
Expose critical-event classifier match basis and ambiguity flags so operators can inspect why retained candidates were routed into a fast-alert class instead of treating the classifier as a black box.

## Backlog review and promotions
- Reviewed `local-fork/BACKLOG.md` first.
- No unscheduled backlog items required promotion this cycle.

## Changes shipped
- Added retained critical-event classification inspection metadata with:
  - winning class id
  - winning and runner-up scores
  - weak-match and ambiguity flags
  - ranked class score summaries
  - top match-basis reasons for the accepted class
- Stored that classification metadata on retained queue candidates so it flows through normal settings and data contracts.
- Extended the critical-event queue contract notes to explicitly describe the new operator-facing classification review metadata.
- Added a new diagnostics card, `Critical event classifier review`, that shows retained candidate score margins, ambiguity flags, and match-basis summaries.
- Kept the classification entry point compatible by leaving `classifyCriticalEventSignal()` returning only the accepted class id while moving deeper inspection into a companion helper.

## Validation
- Tests passed:
  - `node --test test/settings-contract.test.mjs test/settings-endpoint-contract.test.mjs test/ui-payload-contract.test.mjs`
  - result: `pass 11`, `fail 0`
- Live runtime rotation passed:
  - `npm run restart:safe`
  - live listener PID: `59797`
  - replacement mode: `replacement-detected`
  - rotation proved: `true`
- Representative live checks after restart:
  - `/api/health` reported `status: ok`, `ready: true`, current PID `59797`
  - `/api/settings/admin` reported critical-event queue version `critical-event-queue-v1`, four queue notes, and retained candidate classification metadata present

## Backlog and follow-up
- No new unscheduled backlog items were added in this cycle.

## Notes
- This slice improves operator observability only. It does not widen the critical-event taxonomy or alter paging thresholds.
- Ambiguous or weak matches still fail closed and do not enter the retained critical-event queue.
