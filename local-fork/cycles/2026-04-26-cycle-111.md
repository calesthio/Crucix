# Crucix Dev Cycle 111 - Review workflow controls and audit trail

- Date: 2026-04-26
- Plan item: `epic-32 / effort-32.1`
- Status: completed

## Goal
Turn review queues and low-confidence diagnostics into direct operator workflows with acknowledge, snooze, suppress, promote, and quarantine actions plus audit trail capture.

## Changes shipped
- Added review-workflow audit persistence in `server.mjs`:
  - `runs/review-workflow-audit.json`
  - bounded audit retention
  - workflow audit snapshot helpers
- Added review-workflow contract construction in `server.mjs` with:
  - `version = "review-workflow-v1"`
  - `endpoint = "/api/review-workflow/action"`
  - `auditEndpoint = "/api/review-workflow/audit"`
  - queue summary
  - low-confidence source workflow actions
  - promotion candidates
  - recent workflow audit trail
- Extended review queue items with:
  - source provenance
  - dominant source metadata
  - direct workflow actions for source quarantine and suppression when attribution is available
- Added local-admin workflow APIs:
  - `GET /api/review-workflow/audit`
  - `POST /api/review-workflow/action`
- Supported workflow actions:
  - `ack`
  - `snooze`
  - `suppress-source`
  - `quarantine-source`
  - `promote-shadow` (recorded human-review intent with audit capture)
- Updated diagnostics UI in `dashboard/public/diagnostics.html` to render:
  - direct workflow controls
  - recent workflow audit
  - workflow-aware endpoint links
- Exposed review workflow through:
  - `/api/data`
  - `/api/brief/compact`
  - `/api/brief/news/review`
- Expanded regression coverage in `test/review-queue.test.mjs`

## Validation
- Tests passed:
  - `node --check server.mjs`
  - `node --check test/settings-contract.test.mjs`
  - `node --check test/settings-endpoint-contract.test.mjs`
  - `node --test test/settings-contract.test.mjs test/settings-endpoint-contract.test.mjs test/runtime-restart-helper.test.mjs`
- Live validation passed after runtime refresh on `3117`.
- Live `3117` evidence:
  - diagnostics rendered review workflow controls and audit trail
  - `/api/brief/news/review` exposed `workflow.version = "review-workflow-v1"`
  - `/api/brief/news/review` exposed workflow actions and audit entries
  - direct workflow action endpoints were present for local-admin use

## Backlog
- No new backlog item was added this cycle.

## Notes
- Promotion currently records explicit human-review intent and audit history without pretending to silently mutate source-registry lifecycle state from the diagnostics surface.
- This keeps workflow power honest while still making operator decisions visible and actionable.
