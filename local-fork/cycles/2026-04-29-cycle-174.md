# Crucix Dev Cycle 174 - Restore bounded dashboard preference persistence

- Date: 2026-04-29
- Plan item: `epic-57 / effort-57.1`
- Status: completed

## Goal
Restore bounded persistence for dashboard-side panel, map, and workspace controls after the local-admin nonce rollout so operator tweaks do not silently fail across reloads.

## Backlog review and promotions
- Marked `backlog-2026-04-28-settings-concurrency-conflict-ux` as completed because it had already been promoted and shipped as `epic-54 / effort-54.1`.
- Marked `backlog-2026-04-28-extend-local-admin-write-auth` as completed because it had already been promoted and shipped as `epic-55 / effort-55.1`.
- No new backlog items were added in this cycle.

## Changes shipped
- Reworked dashboard preference persistence in `dashboard/public/jarvis.html` so dashboard-side writes first load local admin concurrency and nonce state from `/api/settings/admin`.
- Added bounded client-side batching for rapid panel and layout changes so multiple UI tweaks coalesce into a single persisted write instead of a burst of unauthenticated best-effort calls.
- Updated dashboard persistence writes to send `expectedRevision`, `expectedEtag`, and `X-Crucix-Local-Admin-Nonce`, with a bounded refetch-and-retry path for stale nonce or revision cases.
- Kept the in-memory dashboard preference snapshot aligned with queued and successful writes so follow-up actions use coherent local state.
- Extended served-page smoke coverage to assert the live dashboard ships the admin-settings fetch and local-admin write boundary wiring.

## Validation
Executed:
- `node --test --test-concurrency=1 test/served-page-smoke.test.mjs test/operator-settings-persistence.test.mjs`
- `npm run restart:safe`
- live checks against `http://127.0.0.1:3117/api/health` and `http://127.0.0.1:3117/api/data`

Results:
- Tests passed: `pass 2`, `fail 0`
- Runtime restart passed:
  - previous listener PID: `5384`
  - live listener PID after rollout: `9449`
  - replacement mode: `replacement-detected`
  - rotation proved: `true`
  - listener lookup: `lsof`
- Live endpoint evidence after restart:
  - `/api/health` returned `status: ok`
  - `/api/health` advanced `lastSweep` to `2026-04-29T13:47:56.759Z`
  - `/api/data` returned populated representative arrays: `newsClusters: 57`, `newsFeed: 59`, `suspectSignals: 4`, `corroboratedSignals: 2`

## Runtime note
- The runtime is serving and the restart landed, but `/api/health` still reports `sweepInProgress: true` and some top-level freshness fields remain `null` while `/api/data` is already returning populated news and signal arrays.
- I treated that as pre-existing runtime freshness drift outside this bounded dashboard-persistence slice, not as a blocker to shipping the preference-write fix itself.
