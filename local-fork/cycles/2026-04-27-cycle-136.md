# Crucix Dev Cycle 136 - Added live `/api/analysis` regression coverage for settings-driven publish behavior

- Date: 2026-04-27
- Plan item: `epic-34 / effort-34.4`
- Status: completed

## Goal
Add endpoint-level regression coverage for settings-driven agent-analysis publish behavior so live `/api/analysis` output is validated against operator tuning, not just settings contracts or helper-level logic.

## Changes shipped
- Added `test/analysis-settings-live-contract.test.mjs`.
- The new live test boots real Crucix servers with pre-seeded operator settings and validates `/api/analysis` behavior through the HTTP endpoint.
- Covered live settings-driven publish behavior includes:
  - short-only horizon filtering on published outlook
  - max published tipping-point cap enforcement
  - HIGH-only tipping-point gating under strict policy
  - deterministic-fallback suppression when operator policy disables draft publication and an LLM path is configured but unavailable
- Updated `test/agent-analysis-validation.test.mjs` harness so direct logic tests continue to provide stable coverage with explicit operator-tuning defaults and publish-policy overrides.
- Updated `local-fork/plan.json` to mark `effort-34.4` complete and advance local cycle tracking to cycle 136.

## Validation
- Tests passed:
  - `node --test test/analysis-settings-live-contract.test.mjs test/agent-analysis-validation.test.mjs test/operator-settings-persistence.test.mjs`
- Live runtime validation passed on the actual local Crucix service:
  - `GET http://127.0.0.1:3117/api/health` returned `status = "ok"`, `phase = "serving"`, `dataReady = true`, `pid = 61357`, `port = 3117`
  - `GET http://127.0.0.1:3117/api/analysis` returned `status = "ready"`, `source = "deterministic"`, `refinementState = "completed"`, `outlookCount = 2`, `tippingCount = 1`

## Backlog and roadmap follow-up
- No new backlog items were added.
- The next planned slice is `epic-34 / effort-34.5` for end-to-end regression coverage around `POST /api/review-workflow/action` and `GET /api/review-workflow/audit`.

## Notes
- This cycle primarily strengthens endpoint-level regression protection rather than changing live runtime behavior.
- The added failure-path coverage uses a configured-but-unreachable local Ollama endpoint to validate publish suppression quickly and deterministically.
