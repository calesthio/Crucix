# Crucix dev cycle 023

- Date: 2026-04-24
- Objective: start the next post-Epic-5 roadmap slice by adding a bounded operator ack/dismiss layer for noisy cluster-failure review items, add any further recommendation to the backlog, and validate the live runtime
- Status: completed

## Planned work

1. Confirm the prior roadmap is exhausted and choose the next justified effort from the local dev plan/backlog.
2. Add any new unscheduled recommendation to `local-fork/BACKLOG.md`.
3. Implement the smallest production-meaningful ack/dismiss slice on the runtime.
4. Restart locally, validate health plus review endpoints, and commit the repo.

## Results

- Promoted the next justified roadmap slice into `local-fork/plan.json` as a new Epic 6, `Operator review workflow controls`.
- Marked `effort-6.1` complete:
  - `Bounded operator ack/dismiss layer for noisy cluster-failure regions`
- Added a new unscheduled recommendation to `local-fork/BACKLOG.md`:
  - add ack history counters and recent-dismissal visibility so operators can distinguish a truly quiet review surface from one muted by repeated acknowledgements
- Implemented bounded review acknowledgements in `server.mjs`:
  - added persisted ack storage at `runs/cluster-review-acks.json`
  - added pruning, TTL, and max-entry controls for review acknowledgements
  - annotated cluster review output so active items remain in `review.reviewItems` and acknowledged ones move to `review.dismissedItems`
  - exposed new operator endpoints:
    - `GET /api/brief/news/review/acks`
    - `POST /api/brief/news/review/ack?region=...&reason=...&note=...`
    - `DELETE /api/brief/news/review/ack?region=...&reason=...`
  - added `reviewAcks` health visibility to `/api/health`
- Added config controls in `crucix.config.mjs`:
  - `REVIEW_ACK_TTL_HOURS` (default 72)
  - `REVIEW_ACK_MAX_ENTRIES` (default 100)
- Restarted the local runtime on `http://127.0.0.1:3117` after deployment.

## Evidence

- Syntax validation passed:
  - `node --check server.mjs`
  - `node --check crucix.config.mjs`
- Live runtime health after restart included the new ack summary surface:
  - `/api/health` returned `reviewAcks: { active: 0, maxEntries: 100, ttlMs: 259200000, nextExpiry: null }`
- Live review endpoints returned the new structures:
  - `/api/brief/news/review` returned `dismissedItems`, `dismissedCount`, `activeCount`, and `ackSummary`
  - `/api/brief/news/review/acks` returned `summary` plus `entries`
- Persistence validation passed across restart:
  - temporary validation ack `ValidationRegion :: shape-mismatch` survived a full server restart and reloaded from disk
  - the validation ack was then cleared, and final `/api/brief/news/review/acks` returned `active: 0`
- Representative live runtime verification after restart:
  - `/api/health` returned `status: ok`
  - `lastSweep: 2026-04-24T12:00:23.328Z`
  - `/api/brief/news/review` returned successfully on the fast-start snapshot path

## Notes

- The prior roadmap in `plan.json` was fully completed through Epic 5, so this cycle explicitly promoted the next justified backlog item into a new Epic 6 instead of pretending a pending effort still existed.
- The review ack layer is intentionally bounded and local-operator oriented; it does not attempt multi-user auth or durable audit history yet.
- Next planned work is `effort-6.2`, persist rolling per-region cluster failure stats across restarts so operators can separate chronic weak regions from one-off model misses.
