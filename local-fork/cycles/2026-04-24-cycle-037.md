# Crucix Cycle 037 - 2026-04-24

## Summary
- Completed Epic 8 effort 8.4: runtime maintenance pass for stale OpenSky artifacts and cache-hit telemetry.
- Added OpenSky runtime-state maintenance so expired cache artifacts are pruned instead of being silently resurrected.
- Added cache-hit telemetry and surfaced it through `/api/health` for immediate operator visibility after restart.
- Added deterministic regression tests for stale-cache pruning and cache-hit increments.

## What changed
- Extended `apis/sources/opensky.mjs` to:
  - persist `cacheHits` and `lastCacheHitAt`
  - persist `staleCachePrunes` and `lastStaleCachePrunedAt`
  - prune expired `runs/cache/opensky-latest.json`
  - avoid reusing in-memory stale cache data after prune
  - include `runtimeState` in OpenSky briefing output
- Extended `dashboard/inject.mjs` to carry OpenSky `runtimeState` into `airMeta`.
- Extended `/api/health` in `server.mjs` to publish `openSkyRuntime`, with a direct state-file fallback so telemetry is visible immediately after restart even before a fresh synthesized snapshot lands.
- Added `test/opensky-runtime-maintenance.test.mjs`.

## Validation
- Tests:
  - `node --test /Users/rightclaw/services/crucix/test/opensky-runtime-maintenance.test.mjs /Users/rightclaw/services/crucix/test/source-failure-classification.test.mjs /Users/rightclaw/services/crucix/test/source-counters.test.mjs /Users/rightclaw/services/crucix/test/freshness-policy.test.mjs /Users/rightclaw/services/crucix/test/agent-analysis-validation.test.mjs`
  - Result: `pass 15, fail 0`
- Live runtime after restart on port 3117:
  - `/api/health` returned `status: ok`
  - `openSkyRuntime.cacheHits: 1`
  - `openSkyRuntime.lastCacheHitAt: 2026-04-24T19:21:17.470Z`
  - `openSkyRuntime.queryMode: cooldown-cache`
  - `openSkyRuntime.cooldownUntil: 2026-04-24T19:44:46.684Z`
  - `openSkyRuntime.cacheAgeMinutes: 36.7`
  - `openSkyRuntime.fallback: true`

## Notes
- Found and fixed a real bug during validation: stale cache was being deleted on disk but still reused from the already-loaded in-memory snapshot in the same run.
- Also added a health-route fallback to read OpenSky runtime state from disk, because bootstrapped `latest.json` snapshots can lag the new telemetry fields right after restart.
- No additional backlog item fell out of this cycle.
