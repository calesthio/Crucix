# Crucix Dev Cycle 177 - Telegram and RSS cluster corroboration hardening

- Date: 2026-04-29
- Plan item: `epic-59 / effort-59.1`
- Status: completed

## Goal
Add bounded corroboration and de-duplication between urgent Telegram map candidates and matching RSS/GDELT stories so high-salience OSINT incidents land on the map quickly without permanently inflating single-source weak-cluster pressure.

## Backlog review and promotions
- Reviewed `local-fork/plan.json` and found no remaining ready backlog items to promote.
- Marked `epic-59 / effort-59.1` completed and advanced local cycle state to cycle 177.
- No new backlog items were added in this cycle.

## Changes shipped
- Added bounded post-clustering merge logic in `dashboard/inject.mjs` that merges same-region single-item urgent Telegram clusters with matching single-item RSS/GDELT clusters when headline similarity is high enough.
- Preserved Telegram-led map placement when the merged corroborated cluster includes an urgent Telegram geolocation candidate, so the map stays fast while corroboration can arrive from news.
- Added regression coverage in `test/news-placement-classes.test.mjs` to prove Telegram plus RSS corroboration collapses into one cluster and no longer inflates suspicious near-duplicate weak-cluster pressure.
- Updated `local-fork/plan.json` to reflect completion of the roadmap effort.

## Validation
Executed:
- `node --test --test-concurrency=1 test/news-placement-classes.test.mjs test/news-cluster-near-duplicates.test.mjs test/ui-payload-contract.test.mjs test/local-fork-planning-hygiene.test.mjs`
- `npm run restart:safe`
- live checks against `http://127.0.0.1:3117/api/health` and `http://127.0.0.1:3117/api/data`

Results:
- Tests passed: `pass 10`, `fail 0`
- Runtime restart passed:
  - previous listener PID: `16736`
  - live listener PID after rollout: `20859`
  - replacement mode: `replacement-detected`
  - rotation proved: `true`
  - listener lookup: `lsof`
- Live endpoint evidence after restart:
  - `/api/health` returned `status: ok`
  - `/api/health` returned `lifecycle.phase: serving`
  - `/api/health` returned `lifecycle.dataReady: true`
  - `/api/health` returned `runtimeIdentity.pid: 20859`
  - `/api/data` returned `newsCount: 59`
  - `/api/data` returned `criticalEventQueue.candidates.length: 2`
  - `/api/data` returned top news `placementBasis: telegram-urgent`

## Runtime note
- `/api/health` still reports `sweepInProgress: true` and `lastSweep: null` while the runtime is otherwise serving normally. I treated that as pre-existing runtime freshness behavior outside this bounded Telegram/RSS corroboration slice.
