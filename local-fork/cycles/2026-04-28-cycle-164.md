# Crucix Dev Cycle 164 - Added layout-budget regression coverage to the served dashboard smoke test

- Date: 2026-04-28
- Plan item: `epic-49 / effort-49.1`
- Status: completed

## Goal
Add browser-level regression coverage for the dashboard layout-budget diagnostics so future layout changes fail fast when they remove or weaken the live DOM budget instrumentation that was added to keep the map and panels from freelancing.

## Backlog review and promotions
- Reviewed `local-fork/BACKLOG.md` first.
- Promoted both unscheduled backlog candidates into the roadmap before implementation:
  - `backlog-2026-04-28-admin-preset-browser-regression` -> `epic-58 / effort-58.1`
  - `backlog-2026-04-28-telegram-rss-cluster-corroboration` -> `epic-59 / effort-59.1`
- After promotion, the next pending planned slice in roadmap order was `epic-49 / effort-49.1`.

## Changes shipped
- Extended `test/served-page-smoke.test.mjs` so the booted dashboard smoke test now asserts:
  - the diagnostics workspace preset is present in `workspacePresetLibrary`
  - the diagnostics preset keeps the `layoutBudget` panel pinned and `wide`
  - the served dashboard still includes the live `getLayoutBudgetSnapshot()` instrumentation
  - the layout-budget panel copy still exposes key measured fields and warnings, including:
    - `Topbar+map`
    - `center overflow`
    - `Render budget telemetry`
    - `This is a live DOM budget check, not a promise from memory`
- This gives a browser-served regression net around the dashboard layout-budget surface without changing runtime behavior.

## Validation
- Tests passed:
  - `node --test test/served-page-smoke.test.mjs test/ui-payload-contract.test.mjs`
  - result: `pass 2`, `fail 0`
- This slice is test-only, but the standard live runtime rotation was still re-run afterward as part of the local-fork cycle discipline.

## Backlog and follow-up
- Added `backlog-2026-04-28-layout-budget-headless-measurements`
  - reason: this cycle proves the served page still contains the layout-budget diagnostics and preset wiring, but it does not yet execute a real browser layout pass with threshold assertions against computed ratios.

## Notes
- This is honest coverage, not fake certainty. It catches removal or drift of the served diagnostics surface, but not yet pixel-level browser layout regressions under a real rendering engine.
