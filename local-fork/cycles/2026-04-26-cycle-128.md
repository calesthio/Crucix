# Crucix Dev Cycle 128 - Runtime-bucket drift history persistence

- Date: 2026-04-26
- Plan item: `epic-32 / effort-32.9`
- Status: completed

## Goal
Persist runtime-bucket drift history and delta trends across sweeps so operators can distinguish one-off alias anomalies from sustained contract drift before editing source-registry aliases or promoting a source to expected multi-publisher.

## Changes shipped
- Updated `lib/delta/memory.mjs`
  - persisted compact runtime-bucket drift history inside source-performance snapshots
  - retained drift totals, mismatch counts, alias-gap counts, high-severity count, and top drift items
  - extended reconstructed source-performance history with:
    - per-snapshot `runtimeBucketDrift`
    - per-delta `summaryDelta.runtimeBucketDrift`
    - `topRuntimeBucketShifts` across adjacent sweeps
- Updated `lib/source-ops-runtime.mjs`
  - exposed runtime-bucket drift history fields through the source-ops performance history contract
- Updated `dashboard/public/source-ops.html`
  - surfaced runtime-bucket drift totals in recent snapshots
  - surfaced bucket-drift deltas in latest delta view
  - surfaced `topRuntimeBucketShifts` history in the source-ops panel
- Updated tests:
  - `test/source-performance-history.test.mjs`
  - `test/source-ops-runtime.test.mjs`
  - `test/runtime-history-store.test.mjs`

## Validation
- Tests passed:
  - `node --test test/source-performance-history.test.mjs test/source-ops-runtime.test.mjs test/runtime-history-store.test.mjs`
- Live `3117` evidence:
  - listener rolled to PID `14103`
  - `/source-ops` includes the new history copy for `Bucket drift shifts` and `Top drift buckets`
  - current runtime contract still shows `null` for persisted drift-history values on existing stored snapshots, which is expected because older history was written before this slice persisted runtime-bucket drift fields
  - delta handling remains safe and backward compatible while fresh post-deploy sweeps repopulate the new history slots
- Restart helper behavior:
  - `npm run restart:safe` again exited with `Port 3117 still has a listener after waiting for shutdown`
  - despite that conservative failure, the live listener rolled to the new PID afterward

## Backlog
- No new backlog items were identified in this cycle.

## Notes
- This slice adds historical persistence and drift-delta reconstruction for runtime-bucket attribution anomalies.
- Operators should expect the new historical drift fields to fill in only after new sweeps are captured on the updated build.
