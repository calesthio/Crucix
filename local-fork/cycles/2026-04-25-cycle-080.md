# Crucix dev cycle 080

- Date: 2026-04-25
- Plan item: Epic 25, effort 25.5
- Title: Add a shared source-ops action taxonomy so result envelopes, pruning assessments, and policy evaluators use one canonical set of recommendation labels

## What changed

- Added a new shared action taxonomy artifact:
  - `source-ops/action-taxonomy.json`
- Extended `source-ops/profile.json` so the source-ops contract now advertises:
  - `actionTaxonomyPath = source-ops/action-taxonomy.json`
- Expanded the result-envelope action enum in:
  - `source-ops/schemas/result-envelope.schema.json`
  so result envelopes can reference the same canonical recommendation/action IDs used elsewhere in source-ops.
- Extended pruning artifacts in:
  - `source-ops/schemas/pruning-assessment.schema.json`
  - `source-ops/results/pruning/example-pruning-social-001.json`
  so pruning assessments now expose both:
  - `recommendation`
  - `recommendedAction`
- Extended lifecycle evaluation output in:
  - `lib/source-lifecycle-policy.mjs`
  - `source-ops/results/onboarding-prep/example-lifecycle-evaluation-maritime-001.json`
  so the evaluator now emits a canonical `recommendedAction` in addition to `nextAllowedState` and `blockedReasons`.
- Extended the runtime source-ops surface in:
  - `lib/source-ops-runtime.mjs`
  - `lib/source-scorecard.mjs`
  so `sourceOps` now exposes:
  - `actionTaxonomy.version`
  - `actionTaxonomy.actionCount`
  - `actionTaxonomy.ids`
  - `examplePruning.recommendedAction`
  - `lifecycleEvaluation.recommendedAction`
- Expanded contract/runtime validation in:
  - `test/source-ops-contract.test.mjs`
  - `test/source-ops-runtime.test.mjs`

## Validation

- Passed:
  - `node --check lib/source-scorecard.mjs`
  - `node --check lib/source-lifecycle-policy.mjs`
  - `node --check lib/source-ops-runtime.mjs`
  - `node --check test/source-ops-contract.test.mjs`
  - `node --check test/source-ops-runtime.test.mjs`
  - `node --test test/source-registry.test.mjs test/source-ops-contract.test.mjs test/source-ops-runtime.test.mjs`
- Test result:
  - `pass 14`
  - `fail 0`
- Live runtime validation confirmed against the actual listener on `3117`:
  - `/api/health` returned `status: ok`
  - `/api/health` exposed `sourceOps.actionTaxonomy.version = source-action-taxonomy-v1`
  - `/api/health` exposed `sourceOps.actionTaxonomy.actionCount = 12`
  - `/api/health` exposed `sourceOps.examplePruning.recommendedAction = human-review`
  - `/api/health` exposed `sourceOps.lifecycleEvaluation.recommendedAction = human-review`
  - `/api/data` exposed the same taxonomy and recommended-action fields, including the canonical action ID list

## Runtime note

- The local `3117` restart race happened again during validation.
- I had to verify the real listener PID directly and do a clean kill/restart before the final live proof reflected the latest code.
- This reinforces the already-unscheduled backlog item for a dev-safe restart helper or port-ownership sanity check.

## Backlog follow-up

- No new backlog item was added this cycle.
- Existing unscheduled follow-ups still make sense:
  - schema-level validation helpers for source-ops artifacts
  - dev-safe Crucix restart helper or port-ownership sanity check
- Next planned work is `25.6`: add evaluator support for batch candidate queues so source-ops can compute next allowed transitions and blockers for multiple candidate records at once.
