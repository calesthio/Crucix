# Crucix dev cycle 065

- Date: 2026-04-24
- Plan item: Epic 20, effort 20.1
- Title: Centralize a normalized operator-facing LLM state enum and explanation block in API payloads so dashboard and briefing surfaces consume one shared contract

## What changed

- Added shared API-level builder `buildOperatorLlmStateContract(snapshot, options)`.
- The contract now publishes a versioned operator-state payload:
  - `version`
  - `status`
  - `label`
  - `summary`
  - `configured`
  - `provider`
  - `model`
  - `surfaces.analysis`
  - `surfaces.ideas`
- Added `llmState` to:
  - `/api/data`
  - `/api/brief/compact`
  - `/api/health`
- Preserved `runtimeLlm` as a compatibility alias sourced from the same shared contract so current dashboard code keeps working while the API contract is normalized.
- Restarted the local Crucix runtime so the live service serves the new versioned operator-state payload.

## Validation

- Tests passed:
  - `node --test test/runtime-llm-status.test.mjs test/llm-status-e2e.test.mjs test/review-queue.test.mjs`
- Added regression coverage for the versioned LLM operator-state contract wrapper.
- Live API verification showed all three surfaces publishing the same contract:
  - `/api/data` → `llmState.version = llm-operator-state-v1`, `llmState.status = pending`
  - `/api/brief/compact` → `llmState.version = llm-operator-state-v1`, `llmState.status = pending`
  - `/api/health` → `llmState.version = llm-operator-state-v1`, `llmState.status = pending`
- Compatibility alias remained aligned:
  - `runtimeLlm.status = pending` on all verified surfaces
- Live dashboard verification showed the existing Analysis Review surface still rendering correctly.
- Screenshot captured:
  - `local-fork/artifacts/cycle-065-llm-state-contract.png`

## Backlog follow-up

- No new backlog item added this cycle. The remaining planned work in Epic 20 already covers the next necessary refinements: separating supportability from participation and adding booted-runtime validation.
