# Crucix Dev Cycle 151 - Exposed restart-safe audit timeline on diagnostics and admin surfaces

- Date: 2026-04-27
- Plan item: `epic-42 / effort-42.1`
- Status: completed

## Goal
Promote the retained restart-audit UI backlog item into the roadmap and expose restart-safe audit history as a clearer diagnostics timeline and action-history surface across diagnostics and admin views.

## Changes shipped
- Promoted restart-audit UI work into `local-fork/plan.json` as `epic-42 / effort-42.1` and completed it in the same cycle.
- Updated `dashboard/public/diagnostics.html` to render a dedicated `Restart-safe audit timeline` card that shows:
  - retained entry count and last update time
  - phase, status, replacement mode
  - port, live PID, started PID
  - rotation-proof state
  - error detail when present
- Updated diagnostics endpoint links to include `Runtime history diagnostics`.
- Updated `dashboard/public/admin-settings.html` to render a dedicated `Restart-safe audit history` card and expanded runtime control summary rows for:
  - restart audit entry count
  - latest restart audit timestamp
- Added served-surface regression coverage proving the diagnostics and admin pages contain the new restart-audit wording and links.
- Advanced cycle tracking to 151.

## Validation
- Tests passed:
  - `node --test test/settings-endpoint-contract.test.mjs test/operator-settings-persistence.test.mjs test/ui-payload-contract.test.mjs`
- Live surface verification passed without needing new runtime code changes:
  - `GET /diagnostics` contained `Restart-safe audit timeline`
  - `GET /diagnostics` contained `Runtime history diagnostics`
  - `GET /admin/settings` contained `Restart-safe audit history`
  - `GET /admin/settings` contained `Retained action history`

## Backlog and follow-up
- Added `backlog-2026-04-27-runtime-restart-audit-live-refresh` so queued restart-safe actions can gain poll-backed completion feedback instead of requiring manual reloads.
- Removed `backlog-2026-04-27-runtime-restart-audit-ui` from the unscheduled backlog because it is now promoted and completed as Epic 42.1.

## Notes
- This slice focuses on making retained restart evidence readable from first-class operator surfaces. It does not yet add automatic polling or live completion updates after the restart button is pressed.
