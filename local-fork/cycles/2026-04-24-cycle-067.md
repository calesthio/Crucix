# Crucix dev cycle 067

- Date: 2026-04-24
- Plan item: Epic 20, effort 20.3
- Title: Add a booted-server validation matrix for dashboard and API operator-state combinations under controlled runtime scenarios

## What changed

- Added a booted-runtime regression suite at `test/llm-operator-state-boot-matrix.test.mjs`.
- The new matrix boots real Crucix server instances under controlled env configurations and validates the shared `llmState` contract across:
  - `/api/health`
  - `/api/data`
  - `/api/brief/compact`
- Covered two boot-controlled operator-state scenarios:
  - no provider configured → `LLM UNAVAILABLE`
  - provider configured with ideas path static by design → ideas surface shows `STATIC BY DESIGN` with explicit support/participation semantics
- Fixed env loading in `apis/utils/env.mjs` so explicitly blank environment variables are respected instead of being overwritten by `.env` values. This was required to make booted matrix scenarios genuinely isolated and testable.

## Validation

- Passed:
  - `node --test test/llm-operator-state-boot-matrix.test.mjs test/runtime-llm-status.test.mjs`
- Booted matrix now proves, on real server processes, that:
  - disabled provider boots publish `llmState.status = unavailable`
  - configured boots publish `surfaces.ideas.reason = static-by-design`
  - the shared contract remains versioned as `llm-operator-state-v1`
  - `runtimeLlm.status` stays aligned with `llmState.status`
- Live runtime spot check against the local service still shows the shared contract present on:
  - `/api/health`
  - `/api/data`
  - `/api/brief/compact`
- Screenshot captured:
  - `local-fork/artifacts/cycle-067-booted-llm-matrix.png`

## Backlog follow-up

- Added no new backlog item this cycle.
- Recommendation: the next unscheduled improvement worth adding later, if needed, is a stronger boot matrix for forced fallback and applied paths using injectable test fixtures rather than live sweep behavior. I am leaving that as a recommendation only, not backlog, because the current plan now covers the committed LLM operator-state contract cleanly.
