# Crucix dev cycle 021

- Date: 2026-04-23
- Objective: execute Epic 5 effort 5.2, add any follow-on recommendation to the backlog, and validate the live runtime
- Status: completed

## Planned work

1. Inspect the current Epic 5 hardening path and identify where per-region tuning and retry/backoff telemetry should live.
2. Add any new unscheduled recommendation to the backlog.
3. Implement effort 5.2 on the runtime.
4. Validate live behavior, record the cycle, and commit the repo.

## Results

- Advanced the active roadmap effort in `local-fork/plan.json`:
  - moved `effort-5.2` to `in_progress` at cycle start
- Added a new unscheduled recommendation to `local-fork/BACKLOG.md`:
  - persist rolling per-region cluster prompt stats across restarts if operators need to distinguish one bad sweep from chronic regional failure patterns
- Implemented per-region tuning and retry/backoff telemetry in `dashboard/inject.mjs`:
  - added `NEWS_REGION_TUNING` with region-specific prompt bias and timeout/retry settings for Iran, Israel, India, South Africa, plus a default policy
  - added `getRegionClusterTuning()`
  - cluster prompting now injects per-region guidance before the shared instructions
  - added retry and backoff handling before repair fallback
  - added telemetry fields `retryCount`, `backoffCount`, and `tunedRegionCount`
  - per-region runtime metadata now includes `retried` and the applied tuning settings
- Exposed the new telemetry in `server.mjs`:
  - news cluster summary now returns retry/backoff/tuned-region counts
  - compact briefing text now includes retry count when non-zero

## Evidence

- Pre-change live baseline on `/api/brief/news` showed the shape this effort was meant to improve:
  - `candidateSetCount: 6`
  - `llmSuccessCount: 1`
  - `heuristicFallbackCount: 5`
  - `repairAttemptCount: 5`
  - `repairSuccessCount: 0`
  - per-region fallbacks on Iran, India, Israel, Africa, and Argentina with one success on South Africa
- Startup/live runtime verification after deploy confirmed the new telemetry surface is available immediately:
  - `/api/health` returned `status: ok`
  - `/api/brief/news` responded immediately from the startup snapshot path and now includes `retryCount`, `backoffCount`, and `tunedRegionCount`
  - startup snapshot correctly reports `llm.requestedMode: off` with zero retry/backoff counts, proving the new fields are wired into the live surface without regressing fast startup
- Helper validation confirmed the new metadata shape:
  - `buildNewsClusters(..., { mode: 'force' })` returned per-region tuning metadata including `maxRetries` and `repairTimeout`
- Validation limitation:
  - a direct live `?llm=force` recomputation did not complete within the command timeout window, so this cycle does not claim a fresh live retry rescue result after deployment

## Notes

- This cycle delivered the tuning and telemetry layer, but the runtime still needs better observability for long force-mode recomputes. The next effort should turn persistent failures into operator-facing review surfacing rather than keeping them trapped in debug detail.
- Epic 5 remains active. The next planned work should be `effort-5.3`, operator review surfacing for persistent cluster parse failures.
