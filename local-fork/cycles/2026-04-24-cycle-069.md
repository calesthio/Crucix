# Crucix dev cycle 069

- Date: 2026-04-24
- Plan item: Epic 22, effort 22.1
- Title: Capture sampled high-pressure queue fixtures or snapshots from real runs so prioritization tuning can be validated against realistic operator cases

## What changed

- Added a committed realistic queue fixture at `test/fixtures/review-queue-realistic-sample.json`.
- Sampled the fixture from real local runtime operator surfaces instead of inventing a clean synthetic queue by hand.
- The fixture captures representative high-pressure regions and metrics sampled from live Crucix state, including:
  - persistent review-failure history
  - persistent pressure scores
  - split-pressure metrics
  - suspicious near-duplicate pressure
  - recent repair-artifact distribution
- Updated `test/review-queue.test.mjs` so queue prioritization now runs against the sampled realistic fixture.

## Validation

- Passed:
  - `node --check server.mjs`
  - `node --test test/review-queue.test.mjs`
- Fixture sanity check confirmed:
  - `capturedAt = 2026-04-25T02:45:00Z`
  - `items = 6`
  - expected top prioritized region = `Iran`
- Live source endpoints consulted while building the fixture:
  - `/api/brief/news/review/stats`
  - `/api/brief/news/review/artifacts`
  - `/api/brief/news/review`
- Important honesty note:
  - the current live queue had zero active `reviewItems` because the sampled run was operator-disabled for LLM clustering, so the fixture was constructed from sampled real pressure/state/artifact data rather than copied from a simultaneously non-empty live queue payload.

## Backlog follow-up

- No new backlog item added this cycle.
- Recommendation only, not added: a future capture helper that exports a full queue fixture automatically from runtime state would reduce manual sampling drift if queue-tuning work continues.
