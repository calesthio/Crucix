# Crucix Dev Cycle 185 - Startup synthesis latency visibility and handoff timing

- Date: 2026-04-30
- Plan item: `epic-66 / effort-66.1`
- Status: completed

## Goal
Turn the next obvious startup-synthesis recommendation into the active plan, then expose measurable elapsed-time and handoff-latency fields on `/api/health` so the stuck synthesis path is observable in milliseconds instead of only by phase names.

## Backlog review and promotions
- The backlog had no existing candidate items at cycle start, so I promoted the next bounded recommendation into the roadmap as `epic-66 / effort-66.1`.
- Added follow-on backlog item `backlog-66-a` after live validation showed `lastSuccess.snapshotTimestamp` can still reflect stale published `currentData` while `publishedAt` remains null during a new startup sweep.

## Changes shipped
- Extended `buildRuntimeControlContract()` in `server.mjs` with:
  - `sweep.currentPhaseElapsedMs`
  - `lastSuccess.rawToPersistLatencyMs`
  - `lastSuccess.rawToPublishLatencyMs`
- Mirrored those fields on `/api/health` as:
  - `runtimePhaseElapsedMs`
  - `rawToPersistLatencyMs`
  - `rawToPublishLatencyMs`
- Updated `test/settings-endpoint-contract.test.mjs` so the health contract proves top-level timing fields match the nested runtime-control snapshot.
- Advanced `local-fork/plan.json` cycle state to 185 and completed `epic-66 / effort-66.1`.

## Validation
Executed:
- `node --test --test-concurrency=1 test/settings-endpoint-contract.test.mjs test/sweep-lifecycle-transitions.test.mjs`
- `npm run restart:safe`
- repeated live checks against `http://127.0.0.1:3117/api/health`

Results:
- Tests passed: `pass 10`, `fail 0`
- Restart helper proved listener rotation and healthy serve-up:
  - `livePid: 80031`
  - `rotationProved: true`
  - `healthStatus: ok`
- Live startup health now shows measurable timing for the stuck path:
  - briefing samples reported `runtimePhaseElapsedMs` climbing from `12428` to `23038`
  - synthesis samples reported `runtimePhaseElapsedMs` climbing from `2758` to `13278`
  - synthesis samples exposed `rawToPersistLatencyMs: 5`
  - `rawToPublishLatencyMs` correctly stayed `null` while publish had not succeeded yet
- Live validation also revealed a follow-on honesty issue:
  - `lastSuccess.publishedAt` stayed `null` as expected
  - but `lastSuccess.snapshotTimestamp` still reflected the previously published snapshot timestamp `2026-04-30T12:34:35.711Z`
  - that mismatch is now recorded as `backlog-66-a`

## Follow-on recommendation added to backlog
- `backlog-66-a` Separate stale published snapshot timestamp from in-flight candidate snapshot timing, or explicitly split `publishedSnapshotTimestamp` from `candidateSnapshotTimestamp` on the health contract.
