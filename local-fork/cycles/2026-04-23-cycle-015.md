# Crucix dev cycle 015

- Date: 2026-04-23
- Objective: add selection-memory eviction and prune telemetry for operator health visibility, and update backlog as needed
- Status: completed

## Planned work

1. Add any new unscheduled recommendations to the backlog.
2. Add telemetry for selection-memory prune and eviction causes.
3. Validate the runtime endpoints and commit the cycle.

## Results

- Added a new backlog item for resettable/debug-only selection-memory telemetry controls so long-lived operator sessions can zero counters without restarting the runtime.
- Added runtime telemetry state in `server.mjs`:
  - `selectionMemoryTelemetry.ttlExpired`
  - `selectionMemoryTelemetry.capacityEvicted`
  - `selectionMemoryTelemetry.manualCleared`
  - `selectionMemoryTelemetry.touchHits`
  - `selectionMemoryTelemetry.misses`
- Wired telemetry updates into the existing selection-memory flow:
  - TTL expiry increments on prune and expired recall
  - capacity eviction increments on bounded-cache overflow
  - manual clears increment on explicit context deletion
  - touch hits increment on access refreshes
  - misses increment when a context lookup finds nothing usable
- Extended `selectionMemoryStats()` so health endpoints now expose the telemetry block directly.

## Evidence

- Synthetic helper validation confirmed telemetry updates across all targeted causes:
  - `ttlExpired: 1`
  - `capacityEvicted: 1`
  - `manualCleared: 1`
  - `touchHits: 1`
  - `misses: 2`
- Live runtime verification confirmed the telemetry block is exposed on both:
  - `GET /api/brief/context/health`
  - `/api/health`
- Live runtime verification also exercised:
  - remembered-context touch via `that-one`
  - miss path on a separate context key
  - explicit context clear via `DELETE /api/brief/context`
- Active runtime check:
  - one `server.mjs` process verified with `ps`
  - `/api/health` returned `status: ok`

## Notes

- Telemetry is cumulative for the runtime process lifetime. Because the live runtime had already served prior context-memory requests during development, the exposed counters reflect accumulated activity, not just a single test sequence.
- If post-investigation counter reset becomes useful, the backlog now tracks that follow-on slice.
