# Crucix Dev Cycle 181 - Startup-preview overlap guard for retained sweep-state follow-up

- Date: 2026-04-29
- Plan item: `epic-63 / effort-63.1`
- Status: completed with blocker noted

## Goal
Investigate the retained `sweepInProgress` and stale `sweepStartedAt` behavior seen after publish, focusing on overlap between startup preview analysis and the first live sweep.

## Backlog review and promotions
- Confirmed `backlog-62-a` had already been promoted into `epic-63 / effort-63.1`.
- Added follow-on backlog item `backlog-63-a` after live validation showed the retained sweep gate still reproduces even when startup preview analysis is skipped.

## Changes shipped
- Added `shouldDeferStartupPreviewAnalysis()` in `server.mjs` so startup preview LLM analysis is skipped when a real sweep or another refinement is already active.
- Added `shouldPublishSnapshotUpdate(...)` in `server.mjs` so stale startup-preview analysis results cannot overwrite fresher sweep data if they finish later.
- Updated `enrichAgentAnalysisAndPublish(...)` to support startup-preview mode and avoid stale analysis broadcasts and alert processing.
- Extended `test/sweep-lifecycle-transitions.test.mjs` with regressions that verify:
  - startup preview analysis is deferred while a sweep is active
  - stale startup-preview analysis cannot overwrite fresher sweep data
  - existing sweep lifecycle teardown behavior still passes
- Advanced `local-fork/plan.json` cycle state to 181 and recorded the follow-on backlog item.

## Validation
Executed:
- `node --test --test-concurrency=1 test/sweep-lifecycle-transitions.test.mjs test/sweep-lifecycle-honesty.test.mjs`
- `npm run restart:safe`
- repeated live checks against `http://127.0.0.1:3117/api/health`
- live checks against `http://127.0.0.1:3117/api/data`
- tailed local runtime logs after restart

Results:
- Tests passed: `pass 9`, `fail 0`
- Restart helper proved the updated runtime rotated cleanly:
  - `livePid: 36138`
  - `rotationProved: true`
  - `healthStatus: ok`
- Live runtime evidence confirmed the new startup-preview guard is active:
  - runtime log: `Startup preview analysis skipped because active sweep or refinement is already in flight`
- Live runtime still reproduced the retained-gate issue after publish:
  - `/api/health` returned `status: ok`
  - `/api/health` returned `lifecycle.phase: serving`
  - `/api/health` returned `runtimeIdentity.pid: 36138`
  - `/api/health` later returned `lastSweep: 2026-04-29T15:50:37.347Z`
  - `/api/health` later returned `runtimeControl.lastSuccess.publishedAt: 2026-04-29T15:50:37.347Z`
  - `/api/health` still returned `sweepInProgress: true`
  - `/api/health` still returned `sweepStartedAt: 2026-04-29T15:50:07.200Z`
  - `/api/health` still returned `runtimeControl.sweep.currentPhase: synthesis`
  - `/api/data` returned `newsCount: 59`
  - `/api/data` returned `agentAnalysis.status: ready`
  - `/api/data` still reflected `agentAnalysis.freshness.sweepInProgress: true`

## Blocker / follow-on
- The startup-preview overlap guard landed and is demonstrably active, but it did not fully clear the live retained sweep gate.
- The remaining evidence now points to a separate post-publish/finalization path, because the runtime stayed in `synthesis` with `sweepInProgress: true` after `lastSweep` and `publishedAt` advanced even though startup preview analysis was skipped.
- I recorded this as `backlog-63-a` for the next bounded cycle instead of widening this slice beyond `epic-63 / effort-63.1`.
