# Crucix Dev Cycle 115 - Shared operator surface shell and navigation

- Date: 2026-04-26
- Plan item: `epic-32 / effort-32.5`
- Status: completed

## Goal
Add a shared navigation and page-shell component for dashboard-adjacent operator, diagnostics, admin, and LLM operations surfaces so labels, links, and boundary explanations do not drift.

## Changes shipped
- Added a shared operator surface shell:
  - `dashboard/public/ops-shell.css`
  - `dashboard/public/ops-shell.js`
- Moved the following surfaces onto the shared shell and shared nav:
  - `dashboard/public/settings.html`
  - `dashboard/public/source-ops.html`
  - `dashboard/public/llm-ops.html`
  - `dashboard/public/diagnostics.html`
  - `dashboard/public/admin-settings.html`
- Centralized shared boundary framing, top-level links, and active-surface labeling while preserving each page’s existing data fetch and render behavior.
- Updated regression coverage in:
  - `test/settings-endpoint-contract.test.mjs`
  - `test/operator-settings-persistence.test.mjs`

## Validation
- Tests passed:
  - `node --test test/settings-endpoint-contract.test.mjs test/operator-settings-persistence.test.mjs test/source-ops-runtime.test.mjs`
- Live `3117` evidence:
  - `/settings` served `ops-shell.js` and `activeSurface: 'settings'`
  - `/source-ops` served `ops-shell.js` and `activeSurface: 'source-ops'`
  - `/diagnostics` served `ops-shell.js` and `activeSurface: 'diagnostics'`
  - `/llm-ops` served `ops-shell.js` and `activeSurface: 'llm-ops'`
  - `/admin/settings` served `ops-shell.js` and `activeSurface: 'admin-settings'`
- Restart helper behavior:
  - `npm run restart:safe` again exited with `Port 3117 still has a listener after waiting for shutdown`
  - despite that conservative failure, the live listener rolled to PID `29050` and served the updated shared-shell surfaces afterward

## Backlog
- Added follow-on item in `local-fork/BACKLOG.md`:
  - add browser-level regression coverage for the shared operator surface shell so active-nav state, boundary copy, and admin action availability are validated after client-side render instead of only by static HTML fetches

## Notes
- This slice reduces drift by moving the operator-adjacent surfaces onto one shared shell without changing their page-specific contracts.
- The current regression coverage validates served HTML shell wiring, but not post-render browser behavior; that follow-on is now explicitly tracked.
