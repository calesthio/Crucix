# Crucix Cycle 049 - 2026-04-24

## Summary
- Completed Epic 14 effort 14.1: prevent backlog drift where already-completed recommendations remain advertised after roadmap promotion.
- The prior roadmap was exhausted through Epic 13, and `local-fork/BACKLOG.md` was stale: it still recommended the freshness override validation work that cycle 048 had already shipped.
- Added a regression test so backlog/roadmap drift is caught automatically instead of being rediscovered by the next operator cycle.

## What changed
- Added `test/local-fork-planning-hygiene.test.mjs`.
- The new test asserts:
  - `BACKLOG.md` does not repeat already-completed roadmap effort titles.
  - the integrated-backlog marker does not coexist with recommendation bullet items.
- Cleaned `local-fork/BACKLOG.md` by removing the stale already-shipped recommendation.
- Updated `local-fork/README.md` so the standard dev-cycle flow explicitly says:
  - if the roadmap is exhausted, promote the next justified backlog item into `plan.json` first
  - when a backlog item is promoted and completed, remove it from `BACKLOG.md`
- Added roadmap tracking for this planning-hygiene work under `epic-14` in `local-fork/plan.json`.

## Validation
- Planning hygiene test:
  - `node --test /Users/rightclaw/services/crucix/test/local-fork-planning-hygiene.test.mjs`
  - Result: `pass 2, fail 0`
- Runtime representative checks before restart showed the current `3117` instance was still exposing `activeSourcePolicyCount: 0`, which suggested the live process had not picked up the prior source-policy surface change yet.
- Runtime was therefore restarted for honest deployment verification.

## Notes
- This cycle was process-facing rather than product-surface-facing, but it still mattered because stale backlog state would otherwise keep misleading future `execute next crucix dev cycle` runs.
- Follow-through rule reaffirmed: if runtime evidence shows the live process is stale, restart and re-verify instead of treating repo-only changes as closure.
