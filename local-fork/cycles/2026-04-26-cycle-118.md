# Crucix Dev Cycle 118 - Rolling noise-suppression history

- Date: 2026-04-26
- Plan item: `epic-32 / effort-32.7`
- Status: completed

## Goal
Persist rolling noise-suppression match history and per-rule hit counters so duplicate-burst, low-value, and source-rule suggestions reflect multi-sweep behavior instead of only the current snapshot.

## Changes shipped
- Updated `server.mjs`:
  - added persisted suppression history state at `runs/noise-suppression-history.json`
  - added default/load/save helpers for suppression history
  - added per-sweep history recording via `recordNoiseSuppressionHistory(...)`
  - persisted rolling duplicate-burst history, repetitive-low-value history, and per-source rule hit counters
  - extended `buildNoiseSuppressionContract(...)` to expose:
    - top-level history metadata
    - `historyHitCount`, `firstSeenAt`, and `lastSeenAt` on duplicate-burst items
    - `historyHitCount`, `firstSeenAt`, and `lastSeenAt` on repetitive low-value items
    - hit counters and seen timestamps on active source rules
    - source-rule suggestions ranked from accumulated multi-sweep hits rather than only the current sweep
  - hooked history recording into the sweep path immediately after synthesized source-ops data is built
- Updated `test/cluster-repair-workflow.test.mjs`:
  - extended the extraction harness for suppression-history helpers
  - asserted historical hit counts and rule counters surface in the suppression contract
  - added persistence coverage proving repeated sweeps accumulate counters and write state
- Updated `local-fork/BACKLOG.md`:
  - added follow-on recommendation to bound or decay suppression-history retention so stale one-off bursts and retired sources do not accumulate forever

## Validation
- Tests passed:
  - `node --check server.mjs`
  - `node --test test/cluster-repair-workflow.test.mjs test/settings-endpoint-contract.test.mjs test/settings-contract.test.mjs`
- Live `3117` evidence:
  - listener rolled to PID `38798`
  - `/api/brief/news/review` exposed `workflow.noiseSuppression.version = "noise-suppression-v1"`
  - `/api/brief/news/review` exposed `workflow.noiseSuppression.history.version = "noise-suppression-history-v1"`
  - `/api/brief/news/review` active source rules included `hitCount`, `duplicateBurstCount`, and `repetitiveLowValueCount`
  - `/api/brief/news/review` suggested source rules included `hitCount`
- Restart helper behavior:
  - `npm run restart:safe` again exited with `Port 3117 still has a listener after waiting for shutdown`
  - despite that conservative failure, the live listener did roll and served the updated suppression-history contract afterward

## Backlog
- Added unscheduled follow-on item:
  - bound or decay `runs/noise-suppression-history.json` so stale one-off bursts and retired sources do not accumulate indefinite suggestion weight or unbounded state growth

## Notes
- Current live data had no active duplicate-burst candidates, so the live contract proved the new history envelope and rule-counter fields but not a non-zero duplicate-burst `historyHitCount` sample.
- This slice makes suppression suggestions stateful across sweeps instead of single-snapshot only.
