# Crucix dev cycle 005

- Date: 2026-04-23
- Objective: continue epic 2 by making evidence mode explicit for operator-facing output, especially fresh vs cached vs carried-forward vs degraded
- Status: completed

## Planned work

1. Extend the evidence summary so it exposes explicit evidence modes, not just freshness.
2. Carry OpenSky query-rotation metadata into synthesized output so operator-facing summaries can represent carried-forward evidence honestly.
3. Validate the live runtime payload and commit the cycle.

## Results

- Extended `lib/evidence-summary.mjs` with explicit `evidenceMode` classification.
- Added support for these operator-facing evidence modes:
  - `live`
  - `aggregated`
  - `cached`
  - `fallback-cached`
  - `carried-forward`
  - `degraded-live`
  - `degraded-carried`
- Wired OpenSky rotation metadata into synthesized `airMeta`:
  - `queriedRegions`
  - `queriedRegionCount`
  - `carriedForwardCount`
  - `queryMode`
  - `cooldownUntil` when present
- Updated `evidenceSummary.headline` so operator-facing output now reflects evidence mode inline.

## Evidence

- Direct synth validation returned:
  - `airEvidence.evidenceMode: "carried-forward"`
  - `airEvidence.carriedForwardCount: 6`
  - `airEvidence.queriedRegionCount: 4`
- Live runtime `/api/data` returned:
  - `evidenceSummary.headline: "air:fresh/carried-forward | markets:fresh | telegram:stale | news:stale"`
  - `airMeta.queryMode: "rotating-batch"`
  - `airMeta.carriedForwardCount: 6`
  - matching `tSignals[0]`

## Notes

- One validation pass caught a wiring bug where the evidence summary was using a reduced `airMeta` object and losing carried-forward state. That was corrected before final runtime verification.
- The active runtime on port 3117 is serving the updated payload.
