# Crucix Dev Cycle 178 - Headless layout-budget measurement thresholds

- Date: 2026-04-29
- Plan item: `epic-60 / effort-60.1`
- Status: completed

## Goal
Add true headless-browser layout-budget measurements and threshold assertions so future regressions can verify computed topbar, map, and overflow ratios directly instead of relying only on served-page contract coverage.

## Backlog review and promotions
- Reviewed `local-fork/plan.json` and found no remaining ready backlog items to promote.
- Completed `epic-60 / effort-60.1` and advanced local cycle state to cycle 178.
- No new backlog items were added in this cycle.

## Changes shipped
- Added `test/headless-layout-budget-regression.test.mjs`.
- Booted a real Crucix server in the test, drove the dashboard through `agent-browser`, and captured live `getLayoutBudgetSnapshot()` metrics for:
  - `operator` at `1440x1024`
  - `diagnostics` at `1440x1024`
  - `executive-briefing` at `1920x1080`
- Added bounded threshold assertions for topbar share, map share, first-screen share, remaining viewport, and wallboard virtualization behavior.
- Updated `local-fork/plan.json` to mark Epic 60 complete.

## Validation
Executed:
- `node --test --test-concurrency=1 test/headless-layout-budget-regression.test.mjs test/served-page-smoke.test.mjs test/admin-preset-browser-regression.test.mjs test/ops-shell-browser-contract.test.mjs test/local-fork-planning-hygiene.test.mjs`
- `npm run restart:safe`
- live checks against `http://127.0.0.1:3117/api/health` and `http://127.0.0.1:3117/api/data`

Results:
- Tests passed: `pass 6`, `fail 0`
- Headless layout assertions passed for operator, diagnostics, and executive-briefing presets.
- Runtime restart passed:
  - previous listener PID: `20859`
  - live listener PID after rollout: `25581`
  - replacement mode: `replacement-detected`
  - rotation proved: `true`
  - listener lookup: `lsof`
- Live endpoint evidence after restart:
  - `/api/health` returned `status: ok`
  - `/api/health` returned `lifecycle.phase: serving`
  - `/api/health` returned `lifecycle.dataReady: true`
  - `/api/health` returned `runtimeIdentity.pid: 25581`
  - `/api/health` returned `sourceOps.inventory.total: 30`
  - `/api/data` returned `newsCount: 59`
  - `/api/data` returned `criticalEventQueue.candidates.length: 2`

## Runtime note
- `/api/health` still reports `sweepInProgress: true` and `lastSweep: null` while the runtime is otherwise serving normally. I treated that as a pre-existing runtime freshness behavior outside this bounded layout-regression slice.
