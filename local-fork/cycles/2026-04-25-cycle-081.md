# Crucix dev cycle 081

- Date: 2026-04-25
- Plan item: Epic 25, effort 25.6
- Title: Add evaluator support for batch candidate queues so source-ops can compute next allowed transitions and blockers for multiple candidate records at once

## What changed

- Extended `lib/source-lifecycle-policy.mjs` with a bounded batch evaluator:
  - `evaluateLifecycleBatch(...)`
  - `loadExampleLifecycleBatch(...)`
- The batch evaluator now:
  - maps scorecards and overlap artifacts by candidate ID
  - evaluates multiple candidate records in one pass
  - includes queue-reference awareness from `source-ops/queue/pending.json`
  - reports aggregate counts for:
    - `candidateCount`
    - `queueTaskCount`
    - `blockedCount`
    - `advanceableCount`
- Added an example batch artifact:
  - `source-ops/results/onboarding-prep/example-lifecycle-batch.json`
- Extended `lib/source-ops-runtime.mjs` so `sourceOps` now exposes:
  - `lifecycleBatchEvaluation`
- Expanded validation in:
  - `test/source-ops-contract.test.mjs`
  - `test/source-ops-runtime.test.mjs`

## Validation

- Passed:
  - `node --check lib/source-lifecycle-policy.mjs`
  - `node --check lib/source-ops-runtime.mjs`
  - `node --check test/source-ops-contract.test.mjs`
  - `node --check test/source-ops-runtime.test.mjs`
  - `node --test test/source-registry.test.mjs test/source-ops-contract.test.mjs test/source-ops-runtime.test.mjs`
- Test result:
  - `pass 14`
  - `fail 0`
- Live runtime validation confirmed against the actual `3117` listener:
  - `/api/health` returned `status: ok`
  - `/api/health` exposed `sourceOps.lifecycleBatchEvaluation.version = source-lifecycle-batch-v1`
  - `/api/health` exposed `sourceOps.lifecycleBatchEvaluation.candidateCount = 1`
  - `/api/health` exposed `sourceOps.lifecycleBatchEvaluation.blockedCount = 1`
  - `/api/health` exposed first candidate `candidateId = candidate-maritime-example-001`
  - `/api/health` exposed first candidate `recommendedAction = human-review`

## Runtime note

- The local `3117` restart race happened again.
- Validation required checking the actual bound PID and killing the real listener before the fresh process served the new batch field.
- That keeps the restart-helper backlog item very much alive.

## Backlog follow-up

- No new backlog item was added this cycle.
- Existing unscheduled follow-ups still stand:
  - schema-level validation helpers for source-ops artifacts
  - dev-safe Crucix restart helper or port-ownership sanity check
- Next planned work is `26.1`: classify sources by fusion role such as anchor, corroborator, anomaly detector, context, and exploratory so runtime evidence handling stops treating all feeds alike.
