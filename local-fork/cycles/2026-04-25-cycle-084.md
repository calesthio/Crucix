# Crucix dev cycle 084

- Date: 2026-04-25
- Plan item: Epic 26, effort 26.3
- Title: Measure per-source contribution, overlap, noise, and operator impact so source additions can be validated against cluster quality, review pressure, tipping-point quality, and trust outcomes

## What changed

- Extended `lib/source-ops-runtime.mjs` with a first-class source performance measurement layer exposed as `sourceOps.performance`.
- Added compact per-source metrics including:
  - feed contribution counts
  - suspect and corroborated signal counts
  - degradation signal counts
  - tipping-point citation counts
  - attention score
  - noise score
  - trust outcome label
  - explicit measurement-basis flags
- Added rollup performance targets so source additions can be compared against downstream operator-facing outcomes:
  - cluster quality summary
  - review pressure summary
  - tipping-point count
  - trust outcome distribution
- Kept the surface honest by explicitly marking direct cluster attribution as not yet available per source.
- Expanded regression coverage in `test/source-ops-runtime.test.mjs`.

## Validation

- Passed:
  - `node --test test/source-ops-runtime.test.mjs test/source-registry.test.mjs test/news-cluster-review-metrics.test.mjs test/agent-analysis-validation.test.mjs test/imessage-provenance-brief.test.mjs`
- Test result:
  - `pass 18`
  - `fail 0`
- Live runtime validation confirmed against the actual `3117` listener:
  - `/api/health` returned `status: ok`
  - `/api/health` exposed `sourceOps.performance.totalMeasuredSources = 30`
  - `/api/health` exposed `sourceOps.performance.withSignalContribution = 3`
  - `/api/health` exposed `sourceOps.performance.targets.reviewPressure.lowConfidenceCount = 45`
  - `/api/data` exposed `sourceOps.performance.topImpactSources[0].name = "Telegram"`
  - `/api/data` exposed `sourceOps.performance.topImpactSources[1].name = "GDELT"`

## Runtime note

- The local `3117` restart race happened again during validation.
- A boot attempt still reported port-in-use even though the fresh listener later came up on a different PID.
- Live validation was done against the actual bound process, not assumed from the failed restart handoff.

## Backlog follow-up

- Added one unscheduled recommendation to `local-fork/BACKLOG.md`:
  - direct per-cluster source provenance or cluster-to-runtime-source attribution so contribution scoring stops relying on feed-type proxies like RSS → GDELT
- Next planned work is `26.4`: endpoint-level contract coverage for reasoning metadata on `/api/analysis` and `/api/brief/news`.
