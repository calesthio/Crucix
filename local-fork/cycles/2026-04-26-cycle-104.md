# Crucix Dev Cycle 104 - LLM prompt-debug and reasoning validation surface

- Date: 2026-04-26
- Plan item: `epic-31 / effort-31.2`
- Status: completed

## Goal
Expose prompt-debug, parse-failure, and reasoning-surface validation details for clustering and analysis paths without requiring direct file inspection.

## Changes shipped
- Extended `buildLlmOperationsContract()` in `server.mjs` so `/api/llm/operations` now includes:
  - `clusteringDebug` with review endpoint, artifact endpoint, fallback state, review counts, latest prompt-debug fingerprints, and prompt-preview snippets
  - `parseFailureArtifacts` summary counts for recent clustering repair artifacts
  - `reasoningValidation.analysis` with validation and review endpoints plus published reasoning-surface presence and compact source-reasoning summary
  - `reasoningValidation.news` with published news reasoning-surface presence and compact source-reasoning summary
- Expanded `dashboard/public/llm-ops.html` so the LLM ops page now renders:
  - a dedicated `Clustering prompt debug` card
  - a dedicated `Reasoning surface validation` card
  - direct links to `/api/brief/news/review`, `/api/brief/news/review/artifacts`, `/api/analysis/review`, and `/api/analysis/validation-summary`
- Extended regression coverage in:
  - `test/settings-contract.test.mjs`
  - `test/settings-endpoint-contract.test.mjs`
- Marked `effort-31.2` complete in `local-fork/plan.json` and advanced cycle state to 104.

## Validation
- Tests passed:
  - `node --check server.mjs && node --check test/settings-contract.test.mjs && node --check test/settings-endpoint-contract.test.mjs`
  - `node --test test/settings-contract.test.mjs test/settings-endpoint-contract.test.mjs test/reasoning-endpoint-contract.test.mjs`
- Live validation passed after safe restart:
  - `node scripts/restart-helper.mjs --port 3117`
  - helper restarted owned listener from PID `28560` to PID `66402`
- Live `3117` evidence:
  - `/api/llm/operations` returned `version = "llm-operations-v1"`
  - `/api/llm/operations` returned `clusteringDebug.reviewEndpoint = "/api/brief/news/review"`
  - `/api/llm/operations` returned `clusteringDebug.parseFailureArtifacts.totalArtifacts = 50`
  - `/api/llm/operations` returned `clusteringDebug.promptDebug.promptFingerprint = "1232cb67a4bf2592"`
  - `/api/llm/operations` returned `reasoningValidation.analysis.endpoint = "/api/analysis/validation-summary"`
  - `/api/llm/operations` returned `reasoningValidation.analysis.reasoningSurfacePresent = true`
  - `/api/llm/operations` returned `reasoningValidation.analysis.sourceReasoning.anchorCount = 18`
  - `/api/llm/operations` returned `reasoningValidation.news.reasoningSurfacePresent = true`
  - `/api/analysis` returned `agentAnalysis.sourceReasoning.guidance.cautionRoles = ["exploratory"]`
  - `/api/brief/news` returned `sourceReasoning.guidance.cautionRoles = ["exploratory"]`
  - `/llm-ops` rendered `Clustering prompt debug`, `Reasoning surface validation`, `/api/brief/news/review/artifacts`, and `/api/analysis/validation-summary`

## Notes
- The live runtime already had enough retained repair artifacts to make the new prompt-debug surface immediately useful without manufacturing a failure case.
- No new backlog item was added in this cycle. The next planned work remains `effort-31.3`, agent-analysis tuning controls.
