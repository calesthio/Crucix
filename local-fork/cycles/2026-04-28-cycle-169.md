# Crucix Dev Cycle 169 - Exposed critical-event delivery audit on diagnostics

- Date: 2026-04-28
- Plan item: `epic-52 / effort-52.3`
- Status: completed

## Goal
Expose critical-event delivery audit and resend posture on operator-facing diagnostics so delivery readiness can be reviewed without dropping into raw contract payloads.

## Backlog review and promotions
- Reviewed `local-fork/BACKLOG.md` first.
- No unscheduled backlog items required promotion into the roadmap.

## Changes shipped
- Added a diagnostics card, `Critical event delivery audit`.
- Surfaced critical-event routing and delivery audit summary fields:
  - eligible candidate count
  - retained delivery record count
  - audit update timestamp
  - dispatch mode
  - resend state taxonomy
- Rendered retained delivery records with:
  - candidate label/id
  - class id
  - delivery state
  - last outcome
  - active route plan summary
  - resend eligibility
  - update timestamp
  - retained message preview when present
- Extended diagnostics page coverage so the operator-facing surface now proves the delivery-audit card exists.
- Extended UI payload coverage so the operator-facing settings contract now proves `alerts.criticalEvents.routing.delivery.version` is present.

## Validation
- Tests passed:
  - `node --test test/settings-contract.test.mjs test/settings-endpoint-contract.test.mjs test/ui-payload-contract.test.mjs`
  - result: `pass 11`, `fail 0`
- Live runtime rotation passed:
  - `npm run restart:safe`
  - live listener PID: `64045`
  - replacement mode: `replacement-detected`
  - rotation proved: `true`
- Representative live checks after restart:
  - `/api/health` reported `status: ok`, `ready: true`, current PID `64045`
  - `/api/settings/admin` reported critical-event delivery audit version `critical-event-delivery-audit-v1`
  - live delivery audit currently reports:
    - `dispatchMode: contract-only`
    - `records: 2`
    - `recentRecords: 2`
    - resend states: `preview-only`, `not-eligible`, `ready`, `blocked-no-route`, `sent`, `failed`

## Backlog and follow-up
- No new unscheduled backlog items were added in this cycle.

## Notes
- This slice is observability only. It does not enable outbound delivery.
- The delivery card is intentionally honest about preview-only contract mode and retained resend posture.
