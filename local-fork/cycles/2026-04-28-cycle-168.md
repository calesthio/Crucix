# Crucix Dev Cycle 168 - Exposed critical-event queue transition audit on diagnostics

- Date: 2026-04-28
- Plan item: `epic-52 / effort-52.2`
- Status: completed

## Goal
Expose retained critical-event queue transition audit entries on operator-facing diagnostics surfaces so state changes are reviewable without dropping into raw contract payloads.

## Backlog review and promotions
- Reviewed `local-fork/BACKLOG.md` first.
- No unscheduled backlog items required promotion this cycle.

## Changes shipped
- Added a diagnostics card, `Critical event queue transition audit`, backed by the retained critical-event queue audit already present in the settings contract.
- Surfaced queue audit summary fields:
  - total retained transition entries
  - last update timestamp
  - recent transition count
- Rendered recent transition details with:
  - candidate label/id
  - timestamp
  - status transition string
  - next confidence/status summary
  - promotion basis and unresolved caveats when present
- Extended endpoint/UI coverage so the diagnostics page contract now proves that the transition-audit surface is present.

## Validation
- Tests passed:
  - `node --test test/settings-contract.test.mjs test/settings-endpoint-contract.test.mjs test/ui-payload-contract.test.mjs`
  - result: `pass 11`, `fail 0`
- Live runtime rotation passed:
  - `npm run restart:safe`
  - live listener PID: `62018`
  - replacement mode: `replacement-detected`
  - rotation proved: `true`
- Representative live checks after restart:
  - `/api/health` reported `status: ok`, `ready: true`, current PID `62018`
  - `/api/settings/admin` reported critical-event queue audit version `critical-event-queue-audit-v1`
  - current live dataset had `transitionCount: 0`, so the new surface correctly showed an empty-state audit view instead of inventing entries

## Backlog and follow-up
- No new unscheduled backlog items were added in this cycle.

## Notes
- This slice is observability only. It does not change critical-event queueing behavior or routing thresholds.
- The live dataset currently has no retained transition entries, which is a valid empty state, not a failure.
