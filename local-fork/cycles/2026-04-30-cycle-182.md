# Crucix Dev Cycle 182 - Sweep-state follow-up and startup synthesis honesty tracing

- Date: 2026-04-30
- Plan item: `epic-63 / effort-63.1`
- Status: completed with blocker noted

## Goal
Continue tracing the retained `sweepInProgress` and stale `sweepStartedAt` behavior after restart, and reconcile whether the gate is really staying open after publish or whether the startup sweep is still hanging inside synthesis.

## Backlog review and promotions
- Promoted `backlog-63-a` into the active roadmap for cycle execution under `epic-63 / effort-63.1`.
- Added follow-on backlog item `backlog-64-a` after live validation showed the restarted runtime can remain in `synthesis` with stale `/api/data` while `lastSweep` advances, pointing to deeper startup-sweep tracing and last-success honesty work.

## Changes shipped
- Moved primary sweep finalization ahead of the awaited operational-alert processing path in `server.mjs`, so a slow alert-state save can no longer keep the main sweep gate open.
- Wrapped operational alert processing in a non-fatal promise with explicit logging so alert-state issues do not silently hold the primary sweep lifecycle hostage.
- Extended `test/sweep-lifecycle-transitions.test.mjs` with a regression that proves `runSweepCycle()` clears the primary sweep state before operational alert processing settles.
- Advanced `local-fork/plan.json` cycle state to 182 and promoted the pending backlog item.

## Validation
Executed:
- `node --test --test-concurrency=1 test/sweep-lifecycle-transitions.test.mjs test/sweep-lifecycle-honesty.test.mjs`
- `npm run restart:safe`
- repeated live checks against `http://127.0.0.1:3117/api/health`
- repeated live checks against `http://127.0.0.1:3117/api/data`
- tailed `logs/stdout.log`

Results:
- Tests passed: `pass 10`, `fail 0`
- Restart helper proved listener rotation and healthy serve-up:
  - `livePid: 67477`
  - `rotationProved: true`
  - `healthStatus: ok`
- Live runtime after restart still reproduced the active-sweep symptom, but the evidence shifted:
  - `/api/health` returned `lastSweep: 2026-04-30T11:35:05.826Z`
  - `/api/health` kept returning `sweepInProgress: true`
  - `/api/health` kept returning `runtimeControl.sweep.currentPhase: synthesis`
  - `/api/data` kept returning the older snapshot timestamp `2026-04-30T11:20:30.322Z`
  - `/api/data` kept returning `agentAnalysis.freshness.sweepInProgress: true`
  - `logs/stdout.log` reached `Synthesizing dashboard data...` for the restarted runtime but did not reach the later `Sweep complete` log lines

## Reconciliation / blocker
- The new regression closes one plausible retained-gate path: slow operational-alert processing can no longer keep the primary sweep open in-process.
- Live evidence still shows the restart-time issue, but it now looks less like "publish happened and the gate stayed open" and more like "the startup sweep never finished synthesis/publish, while `lastSweep` still advanced from raw sweep bookkeeping."
- That means the next bounded slice should separate raw sweep completion timing from actual published snapshot success, and add tighter tracing around the startup synthesize/publish handoff.
