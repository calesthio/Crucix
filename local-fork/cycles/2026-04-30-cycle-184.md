# Crucix Dev Cycle 184 - Health contract phase visibility for startup sweep tracing

- Date: 2026-04-30
- Plan item: `epic-65 / effort-65.1`
- Status: completed

## Goal
Promote the remaining backlog recommendation into the active plan, then expose startup sweep phase and raw-to-publish timing directly on `/api/health` so operators can see the stuck state without digging through nested runtime-control fields.

## Backlog review and promotions
- Reviewed the backlog and promoted `backlog-64-b` into the active roadmap as `epic-65 / effort-65.1`.
- After shipping and validation, no additional unscheduled recommendations remained, so the backlog candidate list is currently empty.

## Changes shipped
- Updated `/api/health` in `server.mjs` to build `runtimeControl` once and reuse it consistently.
- Added top-level health fields:
  - `runtimePhase`
  - `runtimePhaseStartedAt`
  - `rawSweepCompletedAt`
  - `rawSnapshotPersistedAt`
- Wired top-level `sweepWatchdog` and `lastSuccess` from the same `runtimeControl` snapshot so the contract stays internally consistent.
- Added endpoint-contract assertions in `test/settings-endpoint-contract.test.mjs` proving:
  - `runtimePhase` matches `runtimeControl.sweep.currentPhase`
  - `runtimePhaseStartedAt` matches `runtimeControl.sweep.currentPhaseStartedAt`
  - top-level raw timing fields mirror `lastSuccess`
- Advanced `local-fork/plan.json` cycle state to 184 and completed `epic-65 / effort-65.1`.

## Validation
Executed:
- `node --test --test-concurrency=1 test/sweep-lifecycle-transitions.test.mjs test/settings-endpoint-contract.test.mjs`
- `npm run restart:safe`
- repeated live checks against `http://127.0.0.1:3117/api/health`
- `tail -n 20 logs/stdout.log`

Results:
- Tests passed: `pass 10`, `fail 0`
- Restart helper proved listener rotation and healthy serve-up:
  - `livePid: 79072`
  - `rotationProved: true`
  - `healthStatus: ok`
- Live startup health now exposes the phase progression directly at top level:
  - samples 1-6 showed `runtimePhase: briefing`, `runtimePhaseStartedAt: 2026-04-30T12:34:05.695Z`, `lastSweep: null`
  - samples 7-8 showed `runtimePhase: synthesis`, `runtimePhaseStartedAt: 2026-04-30T12:34:35.712Z`
  - those same synthesis samples exposed `rawSweepCompletedAt: 2026-04-30T12:34:35.712Z` and `rawSnapshotPersistedAt: 2026-04-30T12:34:35.716Z` while `lastSweep` and `lastSuccess.publishedAt` correctly remained `null`
- Log proof matched the health contract:
  - `logs/stdout.log` reached `Synthesizing dashboard data... raw completed at 2026-04-30T12:34:35.712Z`
  - no later `Sweep complete` line appeared

## Follow-on recommendations
- None added this cycle. The remaining startup issue is still the bounded synthesis hang itself, but no new recommendation was required beyond the completed visibility slice.
