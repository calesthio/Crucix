# Crucix Dev Cycle 171 - Added stale-write recovery UX on admin settings

- Date: 2026-04-28
- Plan item: `epic-54 / effort-54.1`
- Status: completed

## Goal
Add clearer admin-surface stale-write recovery UX so settings concurrency conflicts can be recovered with an explicit reload path instead of leaving operators with only a raw failure string.

## Backlog review and promotions
- Reviewed `local-fork/BACKLOG.md` first.
- No unscheduled backlog items required promotion into the roadmap.

## Changes shipped
- Added bounded admin-page conflict parsing for `settings-revision-conflict` responses.
- Retained current revision/etag details from 409 payloads so the page can explain what changed.
- Added an admin surface card, `Stale-write recovery`.
- Surfaced explicit recovery guidance with:
  - conflict summary text
  - `RELOAD LATEST` action
  - dismiss action
- Updated save/import error handling so stale-write conflicts show recovery-specific wording instead of a generic raw failure string.
- Extended persistence coverage so stale-write responses are proven to expose current revision and etag.
- Extended served-page coverage so the admin surface proves the stale-write recovery card and reload action exist.

## Validation
- Tests passed:
  - `node --test test/operator-settings-persistence.test.mjs test/settings-endpoint-contract.test.mjs test/ui-payload-contract.test.mjs`
  - result: `pass 3`, `fail 0`
- Live runtime rotation passed:
  - `npm run restart:safe`
  - live listener PID: `76579`
  - replacement mode: `replacement-detected`
  - rotation proved: `true`
- Representative live checks after restart:
  - `/api/health` reported `status: ok`, `ready: true`, current PID `76579`
  - `/admin/settings` contained `Stale-write recovery`
  - `/admin/settings` contained `RELOAD LATEST`

## Backlog and follow-up
- No new unscheduled backlog items were added in this cycle.

## Notes
- This slice changes operator recovery UX only. It does not relax concurrency boundaries.
- The admin surface now makes the bounded recovery path obvious without inventing automatic reconciliation.
