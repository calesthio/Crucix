# Crucix Dev Cycle 114 - Noise suppression controls for weak-cluster workflow

- Date: 2026-04-26
- Plan item: `epic-32 / effort-32.4`
- Status: completed

## Goal
Add noise-suppression controls for duplicate bursts, repetitive low-value events, and source-specific suppression rules that can be reviewed and tuned from the UI.

## Changes shipped
- Added operator-settings-backed noise suppression defaults and normalization in `server.mjs`:
  - `preferences.sources.noiseSuppression.duplicateBurst`
  - `preferences.sources.noiseSuppression.repetitiveLowValue`
  - `preferences.sources.noiseSuppression.sourceRules`
- Added `noise-suppression-v1` contract in `server.mjs` with:
  - duplicate-burst candidate detection
  - repetitive low-value candidate detection
  - active source-specific suppression rules
  - suggested source-specific suppression rules
  - bounded tuning controls and summary counts
- Exposed noise suppression through operator and admin settings contracts:
  - `/api/settings`
  - `/api/settings/admin`
  - `sourceConsole.noiseSuppression`
  - `sources.selection.noiseSuppression`
- Extended review workflow and cluster-repair contracts so noise suppression is visible on `/api/brief/news/review` and applied to hide suppressed clusters from the weak-cluster actionable queue.
- Updated UI surfaces:
  - `dashboard/public/admin-settings.html`
  - `dashboard/public/diagnostics.html`
  - `dashboard/public/settings.html`
- Added regression coverage in:
  - `test/cluster-repair-workflow.test.mjs`
  - `test/operator-settings-persistence.test.mjs`
  - `test/settings-contract.test.mjs`
  - `test/settings-endpoint-contract.test.mjs`

## Validation
- Tests passed:
  - `node --test test/settings-contract.test.mjs test/operator-settings-persistence.test.mjs test/settings-endpoint-contract.test.mjs test/cluster-repair-workflow.test.mjs`
- Live `3117` evidence:
  - `/api/settings` exposed `sourceConsole.noiseSuppression.version = "noise-suppression-v1"`
  - `/api/settings` exposed `sources.selection.noiseSuppression.duplicateBurst = { enabled: true, minSimilarClusters: 2 }`
  - `/api/settings` exposed `sources.selection.noiseSuppression.repetitiveLowValue = { enabled: true, maxStoryCount: 1, maxSourceCount: 1 }`
  - `/api/brief/news/review` exposed `workflow.noiseSuppression.version = "noise-suppression-v1"`
  - `/api/brief/news/review` exposed `workflow.clusterRepair.suppression.version = "noise-suppression-v1"`
  - `/diagnostics` rendered `Noise suppression`
  - `/admin/settings` rendered `Noise suppression`
- Restart helper behavior:
  - `npm run restart:safe` again exited with `Port 3117 still has a listener after waiting for shutdown`
  - despite that conservative failure, the live listener rolled to PID `22341` and served the updated noise-suppression contracts afterward

## Backlog
- Added follow-on item in `local-fork/BACKLOG.md`:
  - persist rolling noise-suppression match history and per-rule hit counters so suggestions reflect multi-sweep behavior instead of only the current snapshot

## Notes
- This slice tunes weak-cluster operator workload by filtering clusters that match enabled noise rules before they reach the bounded repair queue.
- The new source-specific rules are UI-tunable and persisted with operator settings, but current suggestions are derived from the active snapshot rather than rolling history.
