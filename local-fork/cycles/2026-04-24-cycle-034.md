# Crucix Cycle 034 - 2026-04-24

## Summary
- Completed Epic 8 effort 8.1: configurable per-source freshness thresholds and policy tuning.
- Moved source-health freshness thresholds and evidence-summary warn windows behind a shared freshness-policy layer.
- Exposed effective policy state through `/api/health` so runtime verification no longer depends on reading source code.
- Added a small regression test file covering source-health and evidence-summary policy metadata.
- Added one backlog recommendation for override-validation coverage against runtime env vars.

## What changed
- Added `lib/freshness-policy.mjs` as the shared policy resolver.
- Updated `lib/source-health.mjs` to read per-source freshness thresholds from shared policy instead of local literals.
- Updated `lib/evidence-summary.mjs` to read per-area warn/stale policy from the same shared policy.
- Added `freshnessPolicy` config wiring in `crucix.config.mjs` with env-driven override points for common sources and areas.
- Extended `/api/health` in `server.mjs` to publish:
  - configured freshness policy
  - active source-health policy summary
  - active evidence-policy summary
- Added `test/freshness-policy.test.mjs`.

## Validation
- Test pass:
  - `node --test /Users/rightclaw/services/crucix/test/freshness-policy.test.mjs /Users/rightclaw/services/crucix/test/agent-analysis-validation.test.mjs`
  - Result: `pass 9, fail 0`
- Live runtime verification after replacing the old process on port 3117:
  - `/api/health` returned `status: ok`
  - `freshnessPolicy.configured.defaultFreshnessMinutes: 60`
  - `freshnessPolicy.configured.sources.OpenSky.freshnessTargetMinutes: 20`
  - `freshnessPolicy.activeSourceHealthPolicy.sourceCount: 29`
  - `freshnessPolicy.activeEvidencePolicy.markets.freshnessWarnMinutes: 20`

## Notes
- First restart attempt failed because the previous Crucix process still owned port 3117. Resolved by killing the port holder and starting the updated server cleanly.
- No additional unscheduled product issue fell out of the implementation beyond the override-validation recommendation added to backlog.
