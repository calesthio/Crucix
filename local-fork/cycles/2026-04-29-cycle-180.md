# Crucix Dev Cycle 180 - Sweep-state closure honesty after publish

- Date: 2026-04-29
- Plan item: `epic-62 / effort-62.1`
- Status: completed with follow-on blocker noted

## Goal
Close the primary sweep state before deferred LLM enrichment work so runtime health stops overstating an active sweep after data is already published.

## Backlog review and promotions
- Reviewed `local-fork/plan.json` and found no remaining backlog items ready for promotion.
- Added follow-on backlog item `backlog-62-a` after live validation showed a deeper retained-gate issue still present post-fix.

## Changes shipped
- Added `finalizeSweepCycle()` in `server.mjs` to centralize sweep gate teardown, phase completion, and freshness sync.
- Moved synthesis job completion and primary sweep finalization earlier in `runSweepCycle()` so the main sweep closes before optional deferred LLM enrichments begin.
- Extended `test/sweep-lifecycle-transitions.test.mjs` with harness updates and a regression that verifies the primary sweep state closes even when deferred enrichments remain unsettled.
- Added `epic-62 / effort-62.1` to `local-fork/plan.json` and advanced cycle state to 180.

## Validation
Executed:
- `node --test --test-concurrency=1 test/sweep-lifecycle-transitions.test.mjs`
- `npm run restart:safe`
- live checks against `http://127.0.0.1:3117/api/health`
- live checks against `http://127.0.0.1:3117/api/data`

Results:
- Tests passed: `pass 5`, `fail 0`
- Restart helper proved listener rotation and healthy serve-up:
  - `livePid: 32189`
  - `rotationProved: true`
  - `healthStatus: ok`
- Live runtime evidence after restart:
  - `/api/health` returned `status: ok`
  - `/api/health` returned `lifecycle.phase: serving`
  - `/api/health` returned `runtimeIdentity.pid: 32189`
  - `/api/health` later returned `lastSweep: 2026-04-29T15:32:23.119Z`
  - `/api/health` later returned `runtimeControl.lastSuccess.publishedAt: 2026-04-29T15:32:23.119Z`
  - `/api/health` later returned `runtimeControl.jobs.analysis.lastOutcome: fallback-empty`
  - `/api/data` returned `newsCount: 59`
  - `/api/data` returned `agentAnalysisStatus: ready`

## Blocker / follow-on
- Despite the new primary sweep finalization, live validation still showed `sweepInProgress: true` and the original `sweepStartedAt` retained after publish while `runtimeControl.sweep.currentPhase` was already `idle` and `lastSuccess.publishedAt` had advanced.
- I recorded this as backlog item `backlog-62-a` for the next cycle because it appears to need deeper runtime tracing beyond this bounded slice.
