# Crucix dev cycle 061

- Date: 2026-04-24
- Plan item: Epic 18, effort 18.3
- Title: Add end-to-end validation for operator-facing LLM status combinations across configured, disabled, fallback, and failure modes

## What changed

- Added `test/llm-status-e2e.test.mjs` to validate operator-facing LLM status combinations across the shared runtime helper and dashboard review surface.
- The new end-to-end matrix covers:
  - configured/applied
  - disabled/unavailable
  - parse-failed fallback
  - timeout/failure fallback with operator-visible error detail retained
- Re-ran the existing runtime helper, dashboard rendering, compact brief, and health-surface tests alongside the new matrix so this cycle closes as a combined status-validation suite instead of a one-off harness.
- Captured a fresh live dashboard screenshot showing the currently published runtime fallback state in the Analysis Review panel.

## Validation

- Test suite passed:
  - `node --test test/llm-status-e2e.test.mjs test/runtime-llm-status.test.mjs test/freshness-policy-runtime-override.test.mjs test/macro-card-rendering.test.mjs test/imessage-provenance-brief.test.mjs`
- Live API verification:
  - `/api/health` returned `runtimeLlm.label = LLM FALLBACK`
  - `/api/brief/compact` returned `runtimeLlm.label = LLM FALLBACK`
- Live dashboard verification:
  - Analysis Review badge: `LLM FALLBACK`
  - Runtime line: `Runtime LLM: LLM FALLBACK • LLM is configured, but published output remains on deterministic or static fallback.`
- Screenshot captured:
  - `local-fork/artifacts/cycle-061-llm-status-e2e.png`

## Backlog follow-up

- Added a recommendation to automate a live server boot-and-render matrix for dashboard/API status combinations, so future operator-state regressions are caught without relying on the currently running local process.
