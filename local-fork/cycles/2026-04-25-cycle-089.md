# Crucix dev cycle 089 - 2026-04-25

## Plan target
- Epic 27, effort 27.3
- Persist operator settings safely so dashboard layout, source toggles, LLM mode, and agent-analysis preferences survive restarts and can be exported or restored.

## What changed
- Added a server-side operator settings store at `runs/operator-settings.json` with normalized defaults and safe merge/import behavior.
- Extended persisted preferences to cover:
  - layout visuals mode, map mode, default region, active layer
  - source selection preferences via enabled categories and enabled source IDs
  - LLM default mode
  - agent-analysis detail level
- Added write/export/import endpoints:
  - `PUT /api/settings/operator`
  - `GET /api/settings/export`
  - `POST /api/settings/import`
- Extended `/api/settings` to expose persistence metadata, persisted preferences, source selection state, and available sources.
- Updated `jarvis.html` to bootstrap real persisted layout preferences and to write back layout changes.
- Updated `settings.html` to save and export preferences, including source category and source-ID toggles.
- Added focused persistence coverage in `test/operator-settings-persistence.test.mjs` and expanded contract tests.

## Validation
- Focused test suite passed:
  - `node --test test/operator-settings-persistence.test.mjs test/settings-contract.test.mjs test/settings-endpoint-contract.test.mjs test/runtime-config-contract.test.mjs`
- Live runtime validation on `http://127.0.0.1:3117` after restart:
  - `PUT /api/settings/operator` saved layout, source, LLM, and agent-analysis preferences
  - `GET /api/settings` returned:
    - `layout.controls.visualsMode = lite`
    - `layout.controls.mapMode = flat`
    - `sources.selection.supportsPerSourceControl = true`
    - `sources.selection.enabledCategories = ["air", "news"]`
    - `sources.selection.enabledSourceIds = ["gdelt-global", "opensky-network"]`
    - `llm.defaultMode = force`
    - `agentAnalysis.controls.detailLevel = expanded`
  - `GET /api/settings/export` returned the persisted preference bundle with the same values

## Notes
- Local restart handoff on port `3117` is still flaky during rapid cycle validation. That remains worth addressing separately under the already-planned restart-helper effort.
- Added a new backlog recommendation for revision or ETag-style concurrency on operator settings writes so stale listeners and conflicting saves are detectable.
