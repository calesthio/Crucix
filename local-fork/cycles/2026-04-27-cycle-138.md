# Crucix Dev Cycle 138 - Added live cluster-repair endpoint regression coverage

- Date: 2026-04-27
- Plan item: `epic-34 / effort-34.6`
- Status: completed

## Goal
Add endpoint-level regression coverage for cluster-repair workflow actions and suppressed-cluster state on `/api/brief/news/review` and `/api/data`, so bounded repair recommendations and persisted decisions do not drift from the live contracts.

## Changes shipped
- Added `test/cluster-repair-endpoint-contract.test.mjs`.
- The new test boots a real Crucix server and validates live cluster-repair workflow behavior exposed through:
  - `GET /api/brief/news/review`
  - `GET /api/data`
  - `POST /api/review-workflow/action`
  - `GET /api/review-workflow/audit`
- The test verifies:
  - review and data payloads both expose `cluster-repair-workflow-v1`
  - bounded cluster-repair actions remain executable through the live action endpoint
  - `suppress-cluster` removes the targeted weak cluster from subsequent workflow payloads
  - `suppressedClusterCount` increments consistently on both `/api/brief/news/review` and `/api/data`
  - recent cluster-repair decisions and workflow audit entries persist after the action is applied
- Updated `local-fork/plan.json` to mark `effort-34.6` complete and advance local cycle tracking to cycle 138.

## Validation
- Tests passed:
  - `node --test test/cluster-repair-endpoint-contract.test.mjs test/review-workflow-endpoint.test.mjs test/cluster-repair-workflow.test.mjs`
- Live runtime validation passed on the actual local Crucix service:
  - `GET http://127.0.0.1:3117/api/health` returned `status = "ok"`, `phase = "serving"`, `dataReady = true`, `pid = 61357`, `port = 3117`
  - `GET http://127.0.0.1:3117/api/brief/news/review` returned `workflow.version = "review-workflow-v1"`, `workflow.clusterRepair.version = "cluster-repair-workflow-v1"`, `workflow.clusterRepair.weakClusterCount = 6`, `workflow.clusterRepair.suppressedClusterCount = 0`
  - `GET http://127.0.0.1:3117/api/data` returned `reviewWorkflow.version = "review-workflow-v1"`, `reviewWorkflow.clusterRepair.version = "cluster-repair-workflow-v1"`, `reviewWorkflow.clusterRepair.weakClusterCount = 6`, `reviewWorkflow.clusterRepair.suppressedClusterCount = 0`, `reviewQueue.state = "empty_elevated_metrics"`, `reviewQueue.totalItems = 0`

## Backlog and roadmap follow-up
- No new backlog items were added.
- The next planned slice is `epic-34 / effort-34.7` for browser-level regression coverage of the shared operator surface shell.

## Notes
- This cycle adds contract protection for persisted cluster-repair suppression state without changing production runtime behavior.
- The live 3117 runtime currently exposes cluster-repair candidates even while the direct review queue remains empty, so this coverage helps keep that split-surface behavior intentional rather than accidental.
