# Crucix Cycle 058 - 2026-04-24

## Summary
- Completed Epic 17 effort 17.3 by adding regression coverage for macro-card formatting and degraded-data rendering across the dashboard and compact briefing path.

## What changed
- Added `test/macro-card-rendering.test.mjs`.
  - Extracts `renderLower()` from `dashboard/public/jarvis.html` into a VM harness.
  - Verifies macro cards render explicit degraded states for missing macro feeds:
    - `Fed Funds -> Not configured`
    - `CPI MoM -> Rate limited`
    - `Unemployment -> Rate limited`
  - Verifies the dashboard output does not leak `NaN`, `NaNT`, `undefined`, or `null`.
  - Verifies healthy values still render alongside degraded states (for example `GSCPI`, `VIX`, and metals).
- Added `test/compact-brief-macro-safety.test.mjs`.
  - Extracts `buildBriefSections()` from `server.mjs`.
  - Verifies the compact brief macro section collapses absent/bad macro values to safe placeholders (`--`) instead of malformed strings.

## Validation
- Targeted regression suite:
  - `node --test test/macro-card-rendering.test.mjs test/compact-brief-macro-safety.test.mjs`
  - Result: `pass 3, fail 0`
- Live dashboard validation at `http://127.0.0.1:3117/` still showed the expected operator-facing macro states:
  - `Fed Funds -> -- / Not configured`
  - `CPI MoM -> -- / Rate limited`
  - `Unemployment -> -- / Rate limited`
- Screenshot saved:
  - `local-fork/artifacts/cycle-058-macro-regression-proof.png`

## Notes
- I did not add backlog items from this cycle because Epic 17 is now exhausted and the next remaining work is already planned under Epic 18.
- The compact brief path does not currently expose the same rich source-state labels as the dashboard macro cards; this cycle locks in placeholder safety there rather than inventing a new compact-state surface outside the plan.
