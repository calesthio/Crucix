# Crucix Dev Cycle 127 - Noise-suppression pressure operator controls

## Summary
- Completed roadmap effort `29.9` from `local-fork/plan.json`.
- Added operator acknowledge and snooze controls for the sustained noise-suppression pressure alert.
- Surfaced recent operational-alert audit visibility through the review workflow contract and diagnostics surface.
- Extended endpoint-level regression coverage for the new control path.

## Implementation details
- Updated `server.mjs` to:
  - persist operational-alert disposition state for acknowledge and snooze actions
  - suppress repeat operational alert dispatch while a noise-suppression pressure alert is acknowledged or snoozed
  - clear alert dispositions automatically once the pressure condition resolves
  - expose `pressureAlert.operatorDisposition` with status, timestamps, actions, and recent audit entries on the review workflow contract
  - accept `ack-noise-suppression-pressure` and `snooze-noise-suppression-pressure` via `POST /api/review-workflow/action`
  - record those actions in `review-workflow-audit`
- Updated `dashboard/public/diagnostics.html` to show operator status and recent alert-audit details for noise-suppression pressure.
- Extended `test/settings-endpoint-contract.test.mjs` to validate the new contract fields, action endpoints, and audit trail.
- Adjusted `test/settings-contract.test.mjs` test harness context so the operational-alert contract helpers remain available under isolated VM extraction.

## Validation
- `node --test test/settings-contract.test.mjs test/settings-endpoint-contract.test.mjs`
  - PASS
- Runtime validation covered by the booted server contract test, including:
  - `GET /api/brief/news/review`
  - `POST /api/review-workflow/action` for both operator control actions
  - `GET /api/review-workflow/audit`

## Backlog
- No new backlog items were added in this cycle.
