# Crucix Dev Cycle 175 - Add dashboard save-status feedback for layout actions

- Date: 2026-04-29
- Plan item: `epic-57 / effort-57.2`
- Status: completed

## Goal
Add explicit dashboard-side save-status feedback for preset and panel-layout actions so operators can tell when a layout action only applied locally versus durably persisted through the supported write boundary.

## Backlog review and promotions
- Reviewed `local-fork/plan.json` and found no remaining unpromoted ready backlog items. The existing backlog entries were already marked completed from earlier promoted work.
- No new backlog items were added in this cycle.

## Changes shipped
- Added a topbar `LAYOUT SAVE` status pill in `dashboard/public/jarvis.html`.
- Wired dashboard preference writes to report bounded states for queued local changes, in-flight durable saves, successful persistence, and local-only fallback when the durable write path fails.
- Styled the status pill so operators can quickly distinguish `PENDING`, `SAVING…`, `PERSISTED`, and `LOCAL ONLY` outcomes without opening admin surfaces.
- Extended served-page smoke coverage to assert the shipped dashboard includes the new save-status surface and copy.

## Validation
Executed:
- `node --test --test-concurrency=1 test/served-page-smoke.test.mjs test/operator-settings-persistence.test.mjs`
- `npm run restart:safe`
- live checks against `http://127.0.0.1:3117/api/health`, `http://127.0.0.1:3117/api/settings/admin`, and `http://127.0.0.1:3117/`

Results:
- Tests passed: `pass 2`, `fail 0`
- Runtime restart passed:
  - previous listener PID: `9449`
  - live listener PID after rollout: `12045`
  - replacement mode: `replacement-detected`
  - rotation proved: `true`
  - listener lookup: `lsof`
- Live endpoint evidence after restart:
  - `/api/health` returned `status: ok`
  - `/api/health` returned `lifecycle.phase: serving`
  - `/api/health` returned `lifecycle.dataReady: true`
  - `/api/health` returned `lastSweep: 2026-04-29T14:00:15.384Z`
  - `/api/settings/admin` returned `persistence.revision: 0`, `persistence.etag: "operator-settings-r0"`, and `admin.writeAuth.required: true`
  - live dashboard HTML at `/` includes `dashboardSaveStatus`, `LAYOUT SAVE`, `LOCAL ONLY`, and `PERSISTED`

## Runtime note
- `/api/health` still reports `sweepInProgress: true` while the runtime is otherwise serving normally. I treated that as pre-existing runtime freshness behavior outside this bounded layout-feedback slice, not as a blocker to shipping the new operator-facing save-status signal.
