# Crucix Dev Cycle 153 - Centralized restart-audit polling in the shared ops shell

- Date: 2026-04-27
- Plan item: `epic-44 / effort-44.1`
- Status: completed

## Goal
Promote the shared restart-audit poller cleanup into the roadmap and remove duplicated per-page polling logic from admin and diagnostics surfaces by moving it into the shared ops shell helper layer.

## Changes shipped
- Promoted shared restart-audit polling cleanup into `local-fork/plan.json` as `epic-44 / effort-44.1` and completed it in the same cycle.
- Updated `dashboard/public/ops-shell.js`:
  - added `createRestartAuditMonitor(shell, options)`
  - centralizes polling timer lifecycle, queued-state refresh scheduling, and completion messaging for restart-audit-driven feedback
  - exports the helper through `window.CRUCIX_OPS_SHELL`
- Updated `dashboard/public/admin-settings.html`:
  - removed inline restart-audit timer helpers
  - now uses `createRestartAuditMonitor(...)` from the shared shell
  - keeps the same operator-visible messages for queued and completed restart-safe outcomes
- Updated `dashboard/public/diagnostics.html`:
  - removed inline restart-audit timer helpers
  - now uses `createRestartAuditMonitor(...)` from the shared shell
  - keeps diagnostics refresh behavior aligned with admin behavior through the shared helper
- Updated served-page contract checks so delivered diagnostics/admin HTML proves the shared helper is referenced.
- Advanced cycle tracking to 153.

## Validation
- Tests passed:
  - `node --test test/settings-endpoint-contract.test.mjs test/operator-settings-persistence.test.mjs test/ui-payload-contract.test.mjs test/ops-shell-browser-contract.test.mjs`
- Live served-page verification passed:
  - `GET /diagnostics` contained `createRestartAuditMonitor`
  - `GET /diagnostics` contained `Restart-safe audit timeline`
  - `GET /diagnostics` contained `polling diagnostics`
  - `GET /admin/settings` contained `createRestartAuditMonitor`
  - `GET /admin/settings` contained `Restart-safe audit history`
  - `GET /admin/settings` contained `polling for completion`

## Backlog and follow-up
- Removed `backlog-2026-04-27-runtime-restart-audit-shared-poller` from the unscheduled backlog because it is now promoted and completed as Epic 44.1.
- Added `backlog-2026-04-27-runtime-action-feedback-contract` so the shared helper can grow from restart-specific polling into a generic queued/completed/failed runtime-action feedback path for stop and future bounded actions.

## Notes
- This is a housekeeping slice with real operator value: same runtime feedback, less per-page drift, and fewer chances for admin/diagnostics wording or timing behavior to diverge later.
