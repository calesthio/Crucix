# Crucix dev cycle 025

- Date: 2026-04-24
- Objective: execute Epic 6 effort 6.3, add sampled hashed repair-failure artifacts for prompt regression comparison, add any follow-on recommendation to the backlog, and validate the live runtime
- Status: completed

## Planned work

1. Advance the next planned effort in Epic 6.
2. Capture sampled, privacy-bounded repair-failure artifacts from the LLM cluster-repair path.
3. Persist those artifacts across restarts and expose them through the operator review surface.
4. Validate live behavior, record the cycle, and commit the repo.

## Results

- Advanced the roadmap in `local-fork/plan.json`:
  - marked `effort-6.3` complete
  - marked Epic 6 complete
  - moved cycle state to `2026-04-24-cycle-025`
- Added a new unscheduled recommendation to `local-fork/BACKLOG.md`:
  - add prompt/version fingerprints to repair-failure artifacts so regressions can be compared across prompt tuning changes without guessing which template generated the bad output
- Implemented sampled hashed repair-failure artifacts in `dashboard/inject.mjs`:
  - added SHA-256 based short hashes for raw, fragment, and repair payloads
  - added bounded preview snippets instead of full raw output retention
  - captured sampled artifacts for:
    - repair responses that still fail schema/parsing
    - repair-attempt exceptions
    - fallback-after-repair cases
  - tracked `repairArtifactCount` in LLM debug output
- Implemented persistence and operator surfacing in `server.mjs`:
  - added persisted state key `cluster-review:repair-artifacts`
  - added retention and max-entry controls driven by config
  - added summarizer for artifact counts, top reasons, top regions, and recent items
  - exposed new endpoint `GET /api/brief/news/review/artifacts`
  - added `clusterRepairArtifacts` visibility to `/api/health`
  - wired sweep-time recording so repair artifacts persist automatically when they occur
- Added config controls in `crucix.config.mjs`:
  - `REPAIR_ARTIFACT_MAX_SAMPLES` (default 12)
  - `REPAIR_ARTIFACT_RETENTION_DAYS` (default 14)
  - `REPAIR_ARTIFACT_MAX_ENTRIES` (default 50)

## Evidence

- Syntax validation passed:
  - `node --check server.mjs`
  - `node --check dashboard/inject.mjs`
  - `node --check crucix.config.mjs`
- Live runtime health after restart included the new artifact summary field:
  - `/api/health` returned `clusterRepairArtifacts`
- New operator endpoint responded successfully:
  - `/api/brief/news/review/artifacts`
- Live runtime naturally showed the persisted region-review stats from the previous cycle working during this run:
  - `/api/health` returned `clusterReviewStats.trackedRegionCount: 6`
- Persistence validation across restart passed with a synthetic artifact record:
  - inserted sample `Iran` repair artifact into hot memory
  - restarted the server
  - `/api/brief/news/review/artifacts` returned:
    - `totalArtifacts: 1`
    - top reason `shape-mismatch`
    - top region `Iran`
    - hashed and preview-only fields, not full raw model output
  - cleared the synthetic artifact and restarted again
  - final `/api/brief/news/review/artifacts` returned `totalArtifacts: 0`

## Notes

- The live runtime did not happen to produce a fresh repair-failure artifact during the direct command window, so the persistence and operator surfacing path was helper-validated with a synthetic artifact and then cleaned up.
- Epic 6 is now complete.
- The next logical work is Epic 7 effort 7.1, trend-memory foundation for multi-day and multi-week operator analysis.
