# Crucix dev cycle 074

- Date: 2026-04-25
- Plan item: Epic 24, effort 24.2
- Title: Implement a structured source scorecard and explainable grading rubric so subagent-produced candidate evaluations are comparable and operator-reviewable

## What changed

- Added a dedicated source scorecard schema at `source-ops/schemas/scorecard.schema.json`.
- Added the shared grading rubric at `source-ops/grading-rubric.json` with:
  - six weighted grading dimensions
  - explicit recommendation thresholds
  - promotion-readiness thresholds
  - operator expectations for reasoning, evidence anchors, and blocking issues
- Added an example grading scorecard at `source-ops/results/grading/example-grading-scorecard.json`.
- Extended `source-ops/profile.json` with canonical paths for:
  - `scorecardSchemaPath`
  - `gradingRubricPath`
- Extended the grading task template so grading work explicitly points to the shared rubric and scorecard schema.
- Added `lib/source-scorecard.mjs` to summarize grading rubric and example scorecard artifacts for operator-facing runtime use.
- Extended `lib/source-ops-runtime.mjs` so `sourceOps` now includes:
  - `grading`
  - `exampleScorecard`
- Expanded validation coverage in:
  - `test/source-ops-contract.test.mjs`
  - `test/source-ops-runtime.test.mjs`

## Validation

- Passed:
  - `node --check lib/source-scorecard.mjs`
  - `node --check lib/source-ops-runtime.mjs`
  - `node --check test/source-ops-contract.test.mjs`
  - `node --check test/source-ops-runtime.test.mjs`
  - `node --test test/source-registry.test.mjs test/source-ops-contract.test.mjs test/source-ops-runtime.test.mjs`
- Test result:
  - `pass 10`
  - `fail 0`
- Live runtime validation after restart confirmed:
  - `/api/health` returned `status: ok`
  - `/api/health` exposed `sourceOps.grading.version = source-grading-rubric-v1`
  - `/api/health` exposed `sourceOps.grading.dimensionCount = 6`
  - `/api/health` exposed `sourceOps.exampleScorecard.recommendation = shadow`
  - `/api/data` exposed `sourceOps.grading.version = source-grading-rubric-v1`
  - `/api/data` exposed `sourceOps.exampleScorecard.promotionReadiness = shadow-ready`

## Runtime note

- The first live endpoint check still reflected the old runtime because the local server had not yet been restarted.
- Restarted the local Crucix server and re-validated against the live listener on port `3117` before recording completion.

## Backlog follow-up

- No new backlog item added this cycle.
- The next planned work is `24.3`: overlap and uniqueness evaluation for candidate sources.
