# Crucix Dev Cycle 172 - Extended local admin nonce coverage across debug-side mutations

- Date: 2026-04-28
- Plan item: `epic-55 / effort-55.1`
- Status: completed

## Goal
Extend the local admin write-auth nonce boundary across additional debug-side mutation endpoints so source controls, review-workflow actions, and runtime control all evaluate under the same local write boundary.

## Backlog review and promotions
- Reviewed `local-fork/BACKLOG.md` first.
- No unscheduled backlog items required promotion into the roadmap.

## Changes shipped
- Enforced local admin nonce validation on:
  - `POST /api/source-ops/control`
  - `POST /api/review-workflow/action`
  - `POST /api/runtime/control`
- Updated source-ops page writes to fetch and send the local admin nonce alongside concurrency tokens.
- Updated diagnostics workflow actions to send the local admin nonce alongside concurrency tokens.
- Updated admin runtime-control actions to send the local admin nonce.
- Extended endpoint coverage so source-control, review-workflow, cluster-repair, and runtime-control regression paths now prove the nonce-gated boundary works under real requests.

## Validation
- Tests passed:
  - `node --test test/operator-settings-persistence.test.mjs test/settings-endpoint-contract.test.mjs test/review-workflow-endpoint.test.mjs test/cluster-repair-endpoint-contract.test.mjs`
  - result: `pass 4`, `fail 0`
- Live runtime rotation passed:
  - `npm run restart:safe`
  - live listener PID: `79808`
  - replacement mode: `replacement-detected`
  - rotation proved: `true`
- Representative live checks after restart:
  - `/api/health` reported `status: ok`, `ready: true`, current PID `79808`
  - `/api/settings/admin` still advertised write-auth header `X-Crucix-Local-Admin-Nonce`
  - `POST /api/runtime/control` with a valid nonce and unsupported action still returned bounded validation (`status 400`, error `Unsupported runtime control action`), proving the request passed auth and reached endpoint validation

## Backlog and follow-up
- No new unscheduled backlog items were added in this cycle.

## Notes
- This slice tightens existing local-only boundaries; it does not broaden write capability.
- Runtime control, source control, and review-workflow mutations now share the same nonce-gated write posture instead of relying on mixed boundary assumptions.
