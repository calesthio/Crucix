services:
  crucix:
    build: .
    ports:
      - "${PORT:-3117}:${PORT:-3117}"
    env_file:
      - .env
    volumes:
      - ./runs:/app/runs
      # Map host's .codex directory to container's root home
      # - ~/.codex:/root/.codex
    restart: unless-stopped
