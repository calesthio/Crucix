services:
  crucix:
    build:
      context: .
      network: host
    network_mode: host
    ports:
      - "${PORT:-3117}:${PORT:-3117}"
    env_file:
      - .env
    volumes:
      - ./runs:/app/runs
    restart: unless-stopped
